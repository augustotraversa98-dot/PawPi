# Night-run batch — 2026-08-12 (READ THIS FIRST)

Five tickets, in order: **2.97** (adoption tab on the business profile), **2.96** and **2.94** (the two
pending from 2026-08-11), then **2.98** and **2.99** (new — activation checklists, owner + business).
Shared conventions from [`docs/phase2-tickets/00-README.md`](phase2-tickets/00-README.md) apply. UNATTENDED
NIGHT RUN.

## ⚡ NIGHT-RUN AUTONOMY (applies to all)
- **Never ask questions.** Ambiguous → take the most sensible, on-brand, reversible option and proceed.
- **Merge policy — AUTO-MERGE:** one fresh branch off `origin/main` per ticket; CI green → **squash-merge
  automatically** → next ticket. CI red → fix, then merge once green. **NOTE:** the 2.96 and 2.94 ticket
  files carry an attended "open a PR and STOP — do not merge" line; **override it tonight: CI green →
  squash-merge → continue.** (2.97/2.98/2.99 are already written for auto-merge.)
- **Keep going down the order** for as long as tokens last; stop cleanly if the limit hits (the checklists
  2.98/2.99 are the biggest, so they're last — if the run ends before them, they're simply next time).
- **Test API behavior through the REAL Hono router by URL** (`api.request('/api/...')`), never by importing a
  handler; no static-vs-`[param]` shadowing; 404 non-integer ids before SQL. **No internal jargon** in
  user-facing copy.
- **Update the bridge docs on every merge** — `docs/roadmap.md` + `PawPi_instructions.md` status block + a
  line in `docs/night-run-log.md`; add Wave 11 index entries to `00-README.md`.
- **No fake data, empty states only.** Mobile is i18n EN+ES neutral "tú".

## Migrations
Only **2.94** adds one. Last applied is **0084** (`provider_post_comments.pet_id`, 2026-08-12), so 2.94's
`provider_post_paws` takes **0085** — next free number at build time (check the dir), SQL block at the top of
the PR body, FLAG in `docs/test-backlog.md` ACTION 1 (Tats hand-applies in the morning). Auto-merge is fine
because the code DEGRADES CLEANLY pre-apply (missing table → 0 paws / no-op like / never a 500), proven in
tests. **2.97, 2.96, 2.98, 2.99 need NO migration** — the two checklists derive completion from existing
data, no new table.

## Build order (recommended)
1. **2.97** — Adoption tab on the business storefront (mobile). Reuses the Adoption-browse card/detail/apply
   design inside the profile. **Do first.**
2. **2.96** — Remove the "Show all sections" nav escape hatch (web).
3. **2.94** — "Paws" (likes) on business posts (mobile + web, migration 0085; degrade-clean + auto-merge +
   flag).
4. **2.98** — Pet-owner activation checklist (mobile): persistent "Getting started" card, derived % to 100%,
   items = first post / set a reminder / log first meal / complete dog history / turn on notifications, plus
   a daily-return local reminder. Biggest item — do after the fixes.
5. **2.99** — Business activation checklist (web provider dashboard): mirror of 2.98, derived % to 100%.
   ⛔ after **2.98** (mirror its checklist component/pattern).

## Not in scope tonight
Daily moments / video for businesses, and a comment-report menu — parked for an attended session.

## Context already verified (so you don't re-derive it)
- 2.97: storefront tabs = `apps/mobile/src/constants/storefrontTabs.js`; browse screen =
  `apps/mobile/src/app/service/adoption.jsx`; `GET /api/adoption/listings` already supports `?provider_id=`.
- 2.94: `provider_posts` has comments (0082)+moderation (0066) but NO paw primitive — mirror the pet feed's
  paw + double-tap (ticket 2.64).
- 2.98/2.99: the checklist building blocks ALL exist — posts, Health **Reminders** (`components/Health/
  Reminders/` + `expo-notifications` scheduling), health/food logs (`0008_health_logs` + nutrition screen),
  the pet medical/history profile (`more/profile-edit.jsx`), notification permission. No activation/checklist
  concept exists yet — build it as derived UI over that data, NO migration. See the tickets for specs.
