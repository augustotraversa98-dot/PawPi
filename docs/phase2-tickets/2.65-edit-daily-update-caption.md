# 2.65 — Edit my daily update's text (fix a typo)

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Web (PATCH route) + mobile. NO
migration. Touches the post detail / posts route — sequence with the other Feed tickets.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile + web
API. Phase-2 ticket 2.65: let an owner EDIT the caption/text of their own daily update (currently a typo
can't be fixed). Read docs/phase2-tickets/00-README.md (persistence + no-fake-data rules; RLS; autonomy).
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/edit-daily-update-caption.
Gates: npm test (web vitest + mobile jest); npm run test:integration if you touch RLS.

CONTEXT
- Daily posts live in the posts table. 2.36 already added an owner-only DELETE at app/api/posts/[id]/route.js
  (posts RLS is owner-write — posts_author_all FOR ALL covers UPDATE too). The post detail modal
  (components/Feed/PostDetailModal.jsx) shows the caption; there's no edit affordance.
- This edits TEXT/caption only — not the photo (the daily photo lock + the unique daily slot are
  untouched). Keep the BeReal daily mechanics intact.

GOAL
On the owner's own post, an Edit affordance lets them change the caption/text; it saves (owner-only,
persisted) and the updated text shows immediately in the detail modal and the feed. Optionally stamp
"edited". No one can edit someone else's post.

SCOPE
- Web: add an owner-only PATCH app/api/posts/[id]/route.js that updates the caption/text field only
  (validate length; ignore/reject attempts to change photo or other fields). Owner-scoped via the existing
  posts RLS (posts_author_all) — confirm UPDATE is owner-only; 401/403 otherwise; 404 for a missing post.
  Optionally set an edited_at/updated_at if the column exists (do NOT add a migration just for this — if
  there's no such column, skip the stamp).
- Mobile: an "Edit" action on the owner's own post (in PostDetailModal, alongside the existing
  delete/share — show only for your own active pet's post) → an inline edit / small modal pre-filled with
  the current text → save calls PATCH → the modal + feed reflect the new text (refetch or update in place).
  Honor the 2.63 keyboard rule (don't auto-pop the keyboard; tap to focus).
- Tests: web vitest (PATCH updates caption owner-only; 401/403 for non-owner; 400 on invalid; photo/other
  fields not mutable); integration only if a policy changed (else reuse the proven posts owner-write);
  mobile jest (edit affordance shown only on own post; save posts the right body; UI reflects the new text;
  not shown on others' posts).

DO NOT: allow editing another user's post; change/replace the photo via this path; break the daily lock /
unique daily slot; add a migration (use existing columns; skip the "edited" stamp if no column exists);
auto-open the keyboard (2.63).

VERIFY: npm test (+ integration if a policy changed) green. PR body: the owner-only caption PATCH + the
edit affordance (own posts only) + the in-place update. DEVICE: open your own daily post → Edit → fix the
text → save → the corrected caption shows in the detail + feed; you cannot edit another pet's post (no edit
affordance); the photo and daily lock are unaffected.
```
