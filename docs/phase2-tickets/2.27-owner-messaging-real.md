# 2.27 — Real owner↔owner messaging (social DMs)

Read `00-README.md` first. Depends on: the chat PATTERN from 2.5 (provider chat). **No hard blocker**;
part of the "feed real data" item. **Product decision locked (Tats): BUILD real owner↔owner messaging**
(not retire). Adds migration **0045** (DM tables). Reuse 2.5's participant-scoped RLS approach, but
between two owners (user_profiles), not owner↔provider.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.27: replace the MOCK social Messages/Chat with a real 1:1
owner↔owner messaging backend. Read docs/phase2-tickets/00-README.md.
Branch ticket/owner-messaging-real off origin/main; squash-merge.
⚙️ NEW MIGRATION 0045 (dm_threads + dm_messages). Gates: npm test (web vitest + mobile jest) + npm run
test:integration (participant-scoped RLS proofs + guard). Confirm baselines first.

CONTEXT (read before editing)
- apps/mobile/src/app/messages.jsx + chat.jsx — TODAY load mockConversations + mockMessages from
  @/data/mockConversationsData. The list + conversation UI is the intended shape — keep it, back it real.
  (This is the SOCIAL chat, SEPARATE from the provider chat app/provider-messages.jsx + provider-chat.jsx,
  which is already real via 2.5 — do NOT merge the two; both can coexist.)
- supabase/migrations/0031_chat_messaging.sql — the REFERENCE pattern: message_threads + messages,
  participant-scoped RLS (visible/writable only to the two parties; sender must be a participant; short-
  poll refresh; unread stamp). Mirror its shape + its RLS proofs, but with TWO user_profiles participants.
- Owner↔owner is a peer relationship — gate on participation (either of the two user_profiles), like the
  provider chat gates on owner-or-staff.

GOAL: two pet owners can hold a private 1:1 conversation (text + optional image), with a Messages list
(unread badges, last message, newest first) and a conversation view (short-poll refresh, mark-read).
Started from a pet/owner profile. Real data only; empty → "No conversations yet".

SCOPE
1. Migration 0045 — dm_threads (id, user_a_id, user_b_id [the two participants, normalized so a pair has
   ONE thread — e.g. store least/greatest + a unique index], last_message_at, created_at) + dm_messages
   (id, thread_id, sender_user_id, body, image_url, read_at, created_at). ENABLE+FORCE RLS, PARTICIPANT-
   SCOPED: a thread is visible to user_a OR user_b (= current_app_user_id()); a message is visible if the
   caller participates in its thread; INSERT requires sender_user_id = current_app_user_id() AND the
   caller is a participant (so no posting as someone else / into a thread you're not in). Harness proofs:
   each participant sees the thread; a third user sees ZERO; can't post as another / into a foreign thread.
   Guard classifies both tables.
2. Routes (web): list my threads; get/create a thread with another user (idempotent on the pair); post a
   message (+ Storage image); mark-read. Mirror the provider-chat routes' shapes.
3. Mobile: messages.jsx (real thread list, unread badges, newest first) + chat.jsx (real conversation,
   short-poll, send text + image, mark-read on open). Start a DM from a pet/owner profile ("Message").
   Remove mockConversationsData seeding.
4. Tests: web vitest (thread create is idempotent per pair; post requires participation; mark-read;
   listing scoped to me); integration (dm participant-RLS proofs + guard); mobile jest (list + conversation
   render real data, send, unread, empty state).

DO NOT: merge with or break the provider chat (2.5) — they're separate; keep mock as a fallback; allow a
non-participant to read/post; create duplicate threads for the same pair (normalize + unique index). No
fake data.

VERIFY: npm test (mobile+web) + integration green (baseline + net-new). PR body: dm_threads/dm_messages +
participant RLS proofs, the routes, the mobile list+conversation, mock removed, provider chat untouched.
⚙️ FLAG FOR TATS: apply 0045. DEVICE PASS: message another owner → real thread, send text+photo, unread
badge clears on open, the other account sees only their side.
```
