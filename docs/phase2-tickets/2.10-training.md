# 2.10 — Training module (classes, 1:1, programs, progress)

Read `00-README.md` + `../phase2-superapp-master-plan.md` (§1 training row) first. Depends on: 2.1, 2.4,
2.3, 2.5. Capability = `trainer`.

```
ORIENTATION
PawPi web API + mobile. Phase-2 ticket 2.10: the TRAINER module — book classes/1:1 sessions, follow a
multi-session program, trainer logs progress; optional group classes + video lessons. Read
docs/phase2-tickets/00-README.md + master plan. Gate with requireProviderCapability(providerId,'trainer').
NOTE: the consumer app already has a "Training" tab (self/content training) — this is the PROVIDER
training service (hiring a trainer), distinct; read the existing Training tab so you don't collide, and
decide whether provider-training surfaces under Services → Training and/or links from the Training tab
(document). Reuse booking (2.4), payments (2.3), chat (2.5), Storage (video lessons).
Branch ticket/service-training off origin/main; squash-merge; migration for programs/sessions/progress.
Gates: npm test + npm run test:integration.

GOAL: a trainer offers classes/1:1/programs; owner books and (for programs) progresses through sessions
with trainer progress notes; group classes support multiple attendees; optional async video lessons.

SCOPE
- Services via provider_services (capability='trainer'): 1:1 session, group class (capacity), program
  (a package of N sessions). Booking via 2.4 (group class = multi-attendee slot; program = a series).
  Fee via 2.3 (programs may be paid up front).
- training_programs / training_sessions / training_progress tables (RLS'd): program enrollment per pet/
  owner, per-session attendance + trainer progress notes (visible to owner), optional video-lesson urls
  (Storage). Provider-staff-scoped writes; owner reads their pet's progress. Harness proofs.
- Group classes: capacity + multiple attendees (reuse the 2.8 capacity pattern).
- mobile: owner — book class/program + see progress + watch video lessons; trainer (web) — manage
  classes/programs + log progress. Discovery ?type=trainer under Services → Training.
- Tests: capability gate; 1:1 + group + program booking; progress notes owner-visible; capacity; RLS.

DO NOT: collide with/duplicate the existing self-Training tab; bypass capability gate; rebuild booking.
No fake data.

VERIFY: npm test + integration green. PR body: program/session/progress model, group capacity, the
Training-tab relationship, RLS. Migration hand-applied after merge. Confirm origin/main.
```
