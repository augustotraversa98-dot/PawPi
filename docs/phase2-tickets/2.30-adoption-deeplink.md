# 2.30 — Adoption per-listing deep-link

Read `00-README.md` first. Depends on: 2.12 (adoption), 2.13 (feed cards). ⛔ **DO NOT START until 2.19
(nav fix) is merged to `origin/main`** — adoption lives in the `more/` stack that 2.19 restructures, and
this ticket deep-links INTO it from the Feed/Discover tab (exactly the cross-tab navigation 2.19 fixes).
**Mobile-only, frontend-only, NO migration** (the listing-detail route + modal already exist). Closes the
roadmap follow-up *"Adoption: per-listing deep-link from feed/discovery (currently opens the adoption hub)."*

```
ORIENTATION
PawPi Expo mobile. Phase-2 ticket 2.30: make the feed/discovery "Adopt me" card open the SPECIFIC dog's
listing, not the adoption hub. Read docs/phase2-tickets/00-README.md.
⛔ PREREQUISITE: 2.19 must be merged (the More-tab nav fix) — build the deep-link against the corrected
navigation. Confirm origin/main has it.
Branch ticket/adoption-deeplink off origin/main; squash-merge. NO migration. Gate: npm test (mobile jest).
Confirm the mobile baseline first.

CONTEXT (read before editing)
- apps/mobile/src/components/Feed/AdoptionFeedCard.jsx — TODAY (2.13) tapping it opens the Adoption hub
  (the browse screen), NOT the specific dog. It already has the listing + place data on the card.
- apps/mobile/src/app/(tabs)/more/adoption.jsx — already has `openListing` state + a `ListingDetailModal`
  (the detail UI EXISTS). It just isn't reachable by a deep param today.
- apps/web/src/app/api/providers/[id]/adoptable-listings/[listingId]/route.js — the single-listing fetch
  (use it if the card needs to (re)load the listing by id).
- The same deep-link should work from Discover (2.25) if it surfaces adoptable dogs.

GOAL: tapping an "Adopt me" card (feed or discovery) opens that exact listing's detail (story, vitals,
fee, apply/favorite/chat) — not the generic hub. Back returns to where it was opened from (respect the
2.19 nav fix; do not corrupt the More tab).

SCOPE
1. AdoptionFeedCard: navigate to the adoption screen WITH the listing identifier (e.g. router.push to the
   adoption route passing { providerId, listingId } as params) instead of the bare hub.
2. adoption.jsx: read the incoming params (useLocalSearchParams). If a listingId is present, open the
   ListingDetailModal for that listing on mount — fetching the single listing via the
   adoptable-listings/[listingId] route if it isn't already in the loaded list (so the deep-link works
   even before the browse list loads / when the dog isn't on the first page). No param → the normal hub,
   unchanged.
3. Navigation hygiene: opening from the Feed tab must NOT corrupt the More tab (this is why 2.19 is the
   prerequisite) — route it the way 2.19 establishes for reaching service/more screens; back returns to
   the origin tab.
4. Empty/stale: if the listing is gone/adopted, show the detail's existing "no longer available" state
   (no crash, no fake).
5. Tests: mobile jest — the card navigates with the listingId param; adoption.jsx opens the detail modal
   for that listing on mount (mock the single-listing fetch); no param → hub as before; the deep-link
   doesn't leave the More tab in a corrupted state (assert the navigation target / reset behavior from
   2.19).

DO NOT: start before 2.19 is merged; add a migration; rebuild the listing detail (reuse ListingDetailModal);
re-introduce the More-tab corruption; show a fake/placeholder dog when the listing is missing.

VERIFY: npm test (mobile) green (baseline + net-new). PR body: the card→specific-listing deep-link, the
param-driven detail open (+ single-listing fetch fallback), nav hygiene built on 2.19, missing-listing
state. DEVICE PASS (docs/test-backlog.md): tap an "Adopt me" card → the exact dog's detail opens → back
returns correctly → More tab still fine.
```
