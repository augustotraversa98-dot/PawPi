# 2.57 — Adoption: foster workflow + urgent/featured flags

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Web + mobile + migration (small).
Picks up the Phase-2 adoption follow-ups. ⛔ Soft-sequence AFTER 2.56 (same adoption listing files);
build 2.56 first if both are in flight.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile +
web API. Phase-2 ticket 2.57: add a foster workflow + urgent/featured flags to adoption listings. Read
docs/phase2-tickets/00-README.md (shared conventions; autonomy preamble). Sequence after 2.56 (shared
adoption files).
AUTONOMY: don't ask; pick the sensible, on-brand option and proceed. CI green → auto squash-merge; red →
fix then merge. One fresh branch off origin/main: ticket/adoption-foster-urgent-flags.
Gates: npm test (web vitest + mobile jest); npm run test:integration (RLS — new columns ride existing
adoption policies; prove no leak).

CONTEXT
- Adoption (2.12) listings + the application workflow + per-listing deep-link (2.30) + public single GET
  (2.56) already exist. Listings have published/available state with public read of published-available
  rows under RLS.
- Two requested follow-ups: (a) FOSTER — a listing (or an application) can be for fostering rather than
  permanent adoption; (b) URGENT/FEATURED — a place can flag a dog as urgent (e.g. medical/at-risk) and/or
  feature it so it surfaces first.

GOAL
Let a rescue/place mark a listing as available for foster (and/or adoption), and flag it urgent and/or
featured; surface those flags in browse/discovery + the feed (urgent badge, featured ordering) and let an
owner indicate foster-vs-adopt intent in the application. Real data + empty states; no fake urgency.

SCOPE
- Migration (next free ≥0055 — check supabase/migrations/ at build time; expected 0055): additive columns
  on the adoptable listings table — placement_type text ['adopt','foster','both'] default 'adopt',
  is_urgent bool default false, is_featured bool default false, urgent_reason text nullable,
  featured_until timestamptz nullable. If the application table needs it: requested_placement text
  ['adopt','foster'] nullable on the adoption application row. These are ADDITIVE columns that RIDE the
  existing adoption RLS (no new table, no policy loosening) — same pattern as 2.20/2.23 additive columns.
  Confirm in the integration harness that the new fields are returned by the published-available public
  read and do NOT leak on non-public rows. Flag in docs/test-backlog.md ACTION 1 (Tats hand-applies).
- Web (provider/rescue dashboard, reuse the shell): the listing editor gains placement_type, an urgent
  toggle + reason, and a featured toggle (+ optional featured_until). The applications view shows the
  applicant's requested_placement.
- Mobile (owner-facing): browse/discovery + the feed Adopt-me cards show an URGENT badge and order
  featured listings first (then by recency); the listing detail shows placement_type (Adopt / Foster /
  Both) + urgent reason; the application form lets the owner pick foster vs adopt when the listing allows
  both. Empty/normal listings render with no badge (no fake urgency).
- Tests: web vitest (editor persists the new fields; featured ordering; applicant placement saved);
  integration (new columns returned on public published-available read, absent/denied on non-public rows;
  completeness guard green); mobile jest (urgent badge renders when flagged + not otherwise; featured
  sorts first; placement choice posts correctly).

DO NOT: loosen adoption RLS or expose non-public listings; fabricate urgency/featured flags; let featured
ordering hide non-featured listings (it reorders, never excludes); break the 2.30/2.56 deep-link or the
2.19 nav. Migrations hand-applied by Tats — flag in test-backlog ACTION 1.

VERIFY: npm test + integration green; completeness guard green. PR body: the additive columns + RLS proof
they ride the existing policy, the dashboard toggles, the urgent badge + featured ordering + foster intent.
DEVICE: flag a dog urgent → an urgent badge shows in browse + feed; feature one → it sorts first; a
foster/both listing lets the applicant choose foster; normal listings show no badge.
```
