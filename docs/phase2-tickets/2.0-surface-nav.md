# 2.0 — Surface "Pet Services" in the nav (mobile)

Read `00-README.md` (shared conventions + capability model) + `../phase2-superapp-master-plan.md` first.

```
ORIENTATION
PawPi mobile (anything/apps/mobile, Expo/Jest). Phase 1 (provider/vet + RLS) is done & RLS is LIVE.
This is Phase-2 ticket 2.0: SURFACE the services side — today the real vet discovery+booking is buried
under More → Pet Services. Read docs/phase2-tickets/00-README.md + docs/phase2-superapp-master-plan.md.
Also read: src/app/(tabs)/_layout.jsx (bottom tabs: Feed, Health, Training, Community, More),
src/app/(tabs)/more/index.jsx (the "PET SERVICES" section → Veterinary/Adoption/Pet Shop), and
src/app/(tabs)/more/vet.jsx (the REAL vet discovery from d1: GET /api/providers/discover?type=vet →
book). adopt.jsx/shop.jsx are mock.
Branch ticket/nav-surface-pet-services off origin/main; squash-merge; mobile only, NO backend/migration.
Gate: npm test (handoff ~627) stays green + your net-new.

GOAL: a primary quick-access "Pet Services" surface (Veterinary live; other categories "coming soon"),
and move Community out of the bottom bar into More.

SCOPE
- (tabs)/_layout.jsx: replace the Community bottom tab with a "Services" tab (new route
  (tabs)/services); keep Feed/Health/Training/More. Lucide icon that reads as services.
- Move Community into More: add a Community MenuItem in more/index.jsx routing to the existing
  Community screen; ensure route/back behavior stays clean on tab switch (heed the known More-tab nav
  caution).
- (tabs)/services/index.jsx: a quick-access category grid (COLORS + lucide cards). Veterinary → LIVE,
  navigates to the real vet discovery (reuse more/vet.jsx — one canonical discovery screen, no dup).
  Grooming / Dog Walking / Daycare & Boarding / Pet Sitting / Training / Shop / Adoption → "Coming
  soon" cards: visible, NOT tappable into a flow. (These map to the master-plan catalog; they light up
  in later tickets.)
- Reconcile the old More → PET SERVICES section: remove the now-duplicate entries OR leave a single
  pointer to the Services tab — exactly one entry point to the real vet discovery.
- Tests (jest + @testing-library/react-native): Services grid renders; Veterinary card navigates to vet
  discovery; coming-soon cards do NOT navigate; Community reachable from More.

DO NOT: build any non-vet service flow; wire payments/reviews/new types; add fake providers behind
coming-soon; change backend or add a migration; break vet discovery/booking; touch Health/Feed/Training.

VERIFY: npm test = baseline + net-new, green. PR body: nav change + only-Veterinary-live + "Phase-2
ticket 2.0". Confirm origin/main has it before the next Phase-2 ticket.
```
