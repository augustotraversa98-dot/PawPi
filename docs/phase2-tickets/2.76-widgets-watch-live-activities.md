# 2.76 — Widgets + Live Activities + Apple Watch (ATTENDED native track)

Read `00-README.md` (shared conventions) first. iOS-native, **multi-phase**, **NO migration**.

⚠️ **THIS TICKET OVERRIDES THE AUTONOMY PREAMBLE.** Unlike Waves 5–7, this is native iOS work that the CI
suites (vitest/jest) CANNOT verify — widgets, Live Activities, and a watchOS app only exist on a real device
via a dev/EAS build. So: **do NOT auto-merge on green CI.** Build each PHASE on its own branch, stop at a
clean reviewable point, and **wait for Tats to test on a device + approve** before merging.

⏳ **APPLE DEVELOPER ACCOUNT ARRIVES IN ~2 DAYS (Tats, set up the day this is written).** Until it lands you
cannot do the account-gated steps (registering an App Group ID, signing/provisioning, enabling capabilities,
EAS credentials, an on-device build). **So WORK AHEAD: build and stage EVERYTHING that does NOT require the
account now, and leave the account-gated steps as clearly-marked, plug-and-play TODOs** so that when the
account is ready it's a short, mechanical finish — not a from-scratch start. See the WORK-AHEAD section below
for exactly what to do now vs. what to defer. Do NOT invent real identifiers (App Group / bundle / team IDs);
use a clearly-labelled placeholder constant in ONE config spot + a TODO, and list every value Tats must fill.

Build the phases IN ORDER, one branch/PR each. Phase 1 delivers value alone; later phases are optional.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo app (SDK 54,
RN 0.81.5, expo-router 6, EAS configured; app.json has expo-build-properties useFrameworks=static;
expo-notifications + expo-location already installed). This is NATIVE iOS: Home/Lock-screen Widgets
(WidgetKit), Live Activities (ActivityKit / Dynamic Island), and an Apple Watch app (watchOS). NONE of this
is testable by jest/vitest — it requires a dev build on a real device.
ATTENDED MODE: do NOT auto-merge. One branch per PHASE off origin/main; stop at a clean point and hand back
to Tats for a device test + merge approval. Ask before any Apple Developer account / signing / capability /
App Group decision — never invent identifiers.
Gates: keep npm test (jest/vitest) green/unchanged for the JS side; the NATIVE acceptance is a manual
device pass (screenshots in the PR).

WORK-AHEAD (do this NOW, before the Apple Developer account lands in ~2 days)
The account ONLY gates: registering the App Group ID, signing/provisioning + capabilities, EAS credentials,
and the actual on-device/EAS build. EVERYTHING ELSE can be built and staged now. Do all of this on the
Phase-1 branch and stop for Tats's review:
- DO NOW (no account needed):
  • Decide + document the native-target approach (recommend `@bacons/apple-targets`); add the dep + config
    plugin wiring; get `expo prebuild` generating the Widget Extension target locally (simulator/codegen
    level — no signing).
  • Write the full SwiftUI widget UI (small + medium + Lock-screen accessory) against a hardcoded SAMPLE
    snapshot struct so it renders in Xcode previews / the simulator without live app data.
  • Write the JS side completely: the pure `buildWidgetSnapshot()` function (from today's reminders, 2.37
    streak, next appointment, active walk/trip), the shared-container writer + WidgetCenter-reload bridge
    (guarded so it no-ops cleanly until the App Group exists), and the widget deep-link → expo-router routes.
    UNIT-TEST the pure snapshot builder + the route resolution in jest now (these ARE CI-testable).
  • Centralize every account-gated value in ONE place — e.g. `native/appGroup.ts` with
    `APP_GROUP_ID = "group.PLACEHOLDER.pawpi"  // TODO(Tats): real App Group ID` — and reference it
    everywhere, so flipping to the real ID is a one-line change.
  • Create docs/native-widgets.md describing: the target setup, the App Group + snapshot flow, and a
    "FINISH WHEN THE APPLE ACCOUNT IS READY" checklist (every identifier + capability + EAS step + the exact
    files/lines to edit). This is the plug-and-play handoff.
- DEFER (needs the account; leave as the documented checklist, do NOT fake):
  • Register the real App Group ID + bundle IDs under the team; enable the App Groups (and later Push /
    Live Activities) capabilities; set up EAS signing credentials; run the first real dev/EAS BUILD on a
    device; do the on-device acceptance pass.
- When the account lands, the remaining work should be: paste the real IDs into the one config spot, enable
  capabilities, EAS build, device-test — then merge. Flag clearly in the PR that it's "staged, pending Apple
  account" so Tats knows it's review-not-merge until then.

CONTEXT / CONSTRAINTS
- Expo managed app → native extensions need Continuous Native Generation (prebuild) + a config plugin that
  adds the native targets. Recommended tooling: `@bacons/apple-targets` (Evan Bacon's Expo Apple-targets
  plugin) to add a Widget Extension (and later a Watch app) as SwiftUI targets inside the Expo prebuild, OR
  a hand-written config plugin. Confirm the approach with Tats before adding the dep. EAS build is required
  (Expo Go canNOT run widgets/Live Activities/Watch).
- Data path: the RN app must SHARE data with the widget/Watch. Use an App Group (shared UserDefaults / a
  shared container) that the RN app writes a small JSON "snapshot" into (today's reminders, posting streak,
  next appointment, any in-progress walk/transport), which the widget reads. The App Group ID + entitlement
  is an Apple-account step — ASK Tats. Do NOT have the widget hold the user's auth token if avoidable;
  prefer the app writing snapshots. Live Activities update via the RN app (ActivityKit start/update/end) and
  optionally APNs push later.
- Reuse the REAL data the app already computes — Health → Today reminders, the 2.37 posting streak, the next
  vet appointment, the 2.70 transport live trip, the 2.7 walk session. No fake data on any surface; show a
  clean empty/"open PawPi" state when there's nothing.
- Brand: the warm PawPi palette (coral #FF6F61, peach, warm brown) + the paw motif; match the app.

PHASE 1 — Home/Lock-screen Widget (WidgetKit) [DO FIRST]
- Add the Widget Extension target via the chosen plugin; prebuild + EAS dev build works on device.
- Set up the App Group + a tiny snapshot writer in the RN app: on relevant changes (app foreground, after
  logging/posting), write a JSON snapshot { petName, petPhoto?, todayReminders:[{title,time,done}],
  streakDays, nextAppointment?, activeWalkOrTrip? } into the shared container; call WidgetCenter reload.
- SwiftUI widget (small + medium, + a Lock-screen accessory): show the pet name/avatar, today's next
  reminder(s) + done count, and the streak. Tapping deep-links into the app (widget URL → expo-router route).
  Empty state ("Open PawPi to get started") when no snapshot.
- Acceptance (device): add the widget → it shows the real pet + today's reminders + streak; completing a
  reminder in-app updates the widget; tap deep-links to the right screen; empty state when logged out/no pet.

PHASE 2 — Live Activity for an in-progress walk / transport (ActivityKit) [⛔ after Phase 1; soft-needs 2.70]
- Add the ActivityKit attributes + a Live Activity UI (Lock Screen + Dynamic Island compact/expanded).
- The RN app starts a Live Activity when a walk (2.7) or transport trip (2.70) goes in-progress, updates it
  as status/location/ETA changes, and ends it on completion. (Local updates first; APNs push updates can be
  a follow-up.)
- Acceptance (device): start a walk/trip → a Live Activity appears on the Lock Screen + Dynamic Island with
  live status; it updates as the activity progresses; it ends/clears on completion. Degrades cleanly on
  devices without Dynamic Island.

PHASE 3 — Apple Watch app (watchOS) [⛔ after Phase 1; LARGEST — its own multi-PR effort]
- Add a watchOS app target (SwiftUI). v1 scope (confirm with Tats): a glanceable Today view (next
  reminders + done count + streak) reading the shared snapshot via WatchConnectivity / shared container, a
  complication showing the streak or next reminder, and a "mark done" action that round-trips to the phone.
- Acceptance (device + paired Watch): the Watch shows today's reminders + streak; a complication renders;
  marking a reminder done on the Watch reflects on the phone. (This phase likely spans several PRs — checkpoint
  with Tats.)

CROSS-CUTTING
- Keep ALL JS-side changes (snapshot writer, deep-link routes, ActivityKit start/stop bridge) covered by jest
  where testable (e.g. the snapshot builder is a pure function — unit-test it; the deep-link route resolves).
  The native SwiftUI + on-device behavior is verified by Tats's device pass + PR screenshots, NOT CI.
- New native deps / the App Group / signing / capabilities are Apple-account steps — ASK Tats; document the
  exact identifiers in the PR and in docs (a new docs/native-widgets.md describing the target setup + App
  Group + how the snapshot flows, so the next chat is oriented).
- i18n: any user-facing widget/Watch strings go through the same EN+ES catalog approach where feasible.

DO NOT: auto-merge on green CI (attended only — wait for Tats's device approval per phase); STALL waiting for
the Apple account — build + stage everything that doesn't need it now (WORK-AHEAD), defer only the
account-gated steps as a documented checklist; invent real App Group / bundle / team IDs (use ONE labelled
placeholder constant + TODO); ship the auth token into the widget if a snapshot suffices; show fake data on
any USER-FACING surface (the SwiftUI sample snapshot is for Xcode previews only, empty states in the app);
attempt this in Expo Go (needs a dev/EAS build); collapse all three phases into one PR. NO database migration.

VERIFY (per phase): the JS-side units (snapshot builder, deep-link routes, ActivityKit bridge calls) are
jest-green; the PR includes DEVICE SCREENSHOTS of the surface working on real hardware; Tats does the device
pass and approves the merge. PR body: which phase, the native target/plugin added, the App Group + snapshot
flow, the data surfaces shown (real data + empty states), and the device-test result.
```
