# 2.41 — Vet Record: owner can complete info + upload documents

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + web API. Covers #7. `vet_documents`
(owner-only) already exists from the RLS arc — likely no migration.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.41: let the OWNER fill in their pet's vet-record info and
UPLOAD documents. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5. One branch off origin/main.
Branch ticket/vet-record-owner-upload. Gates: npm test (web vitest + mobile jest); npm run
test:integration if you touch RLS.

CONTEXT
- apps/mobile/src/components/Health/HealthVetRecord.jsx + components/Health/VetRecord/* — the Vet Record
  hub (Pet Medical Profile, allergies, conditions, meds, vaccinations, documents, vet notes, etc.).
  Project rule: data-driven, no fake data, empty states.
- vet_documents is an OWNER-scoped table (RLS arc R2c) — the home for uploaded documents. Pet medical
  profile + allergies/conditions tables also exist and are owner-writable.
- Supabase Storage upload path (reused app-wide) for the document files.

GOAL: the owner can complete/edit the medical-relevant fields on their pet and upload documents (lab
results, records, photos of paperwork) into the Vet Record, all persisted and shown back (real data,
empty states when none).

SCOPE
- Document upload: wire an "Add document" flow in the Documents section → Storage upload → insert a
  vet_documents row (owner-scoped) with a name/type + the file URL + date; list them (newest first) with
  open/view + delete (owner). Empty state when none.
- Complete info: ensure the owner can add/edit the owner-editable Vet Record fields (pet medical profile
  fields + allergies / known conditions / current meds entries that are owner-writable) through real
  create/edit/delete that persists + refetches. Fill any gaps where a section is currently read-only or
  mock.
- Keep the not-a-diagnosis framing intact.
- Tests: web vitest (document create/list/delete owner-scoped; field edits persist); integration
  (vet_documents + the edited tables stay owner-RLS correct — owner writes, others zero); mobile jest
  (upload flow + sections render real data + empty states).

DO NOT: show fake records; let another user read/write this pet's documents (owner-scoped RLS);
present the app as diagnostic; add a migration unless a genuinely missing column is needed (vet_documents
already exists — check first).

VERIFY: npm test (+ integration) green. DEVICE: upload a document → it appears + opens; edit medical info →
persists across reopen; empty sections show empty states.
```
