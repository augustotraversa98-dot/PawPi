# 2.33 — Notifications filter chips layout fix

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile-only, frontend-only, no migration. Trivial
UI bug. Independent.

```
ORIENTATION
PawPi Expo mobile. Phase-2 ticket 2.33: fix the notifications filter chips rendering as tall vertical
rectangles. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5 until tokens run out. One branch off origin/main. NO migration.
Branch ticket/notif-chips-fix. Gate: npm test (mobile jest).

CONTEXT
- apps/mobile/src/app/notifications.jsx — the FILTER_OPTIONS chips (All / Walks / Feeding / Paws / Barks /
  Training). They currently render as overly tall vertical rectangles instead of compact pills.

GOAL: the filter chips render as a tidy horizontal row of compact pills (wrapping or horizontally
scrollable), consistent with the app's other chip rows.

SCOPE
- Fix the chip container/row styling so chips size to their content (horizontal layout, proper padding,
  no stretched height). If the row can overflow, make it a horizontal ScrollView or wrap. Match the
  existing chip styling used elsewhere (e.g. the adoption/community tabs) for consistency.
- Tests: a light render test asserting the chips render in a row (or just that the screen renders without
  regression) — keep it minimal; this is a style fix.

DO NOT: change the filtering logic or the notifications data; add a migration; restyle unrelated screens.

VERIFY: npm test (mobile) green. PR body: before/after of the chip layout fix. DEVICE: chips read as a
neat pill row.
```
