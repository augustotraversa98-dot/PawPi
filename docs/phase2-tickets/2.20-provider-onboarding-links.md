# 2.20 — Provider onboarding: capture business links

Read `00-README.md` + `../phase2-superapp-master-plan.md` first. Depends on: 2.1. ⛔ **DO NOT START until
2.15 (mobile capability multi-select) is merged to `origin/main`** — this ticket edits the SAME mobile
onboarding form (`vet-business-access.jsx`). ⚠️ **Shared-file note:** 2.18 (telehealth) also edits that
form; build 2.15 → 2.18 → 2.20 **sequentially** (never two of them in parallel branches). Adds migration
**0041** (link columns on `providers`).

```
ORIENTATION
PawPi web API + Expo mobile. Phase-2 ticket 2.20: capture a provider's public LINKS at onboarding/profile
(website, Instagram, Facebook, Google Maps). Read docs/phase2-tickets/00-README.md.
⛔ PREREQUISITE: 2.15 must be merged (the mobile onboarding multi-select). Confirm origin/main has it.
⛔ SHARED FILE: 2.18 (telehealth) also edits vet-business-access.jsx — if 2.18 is still open/unmerged,
finish it FIRST. Never run 2.18 and 2.20 as parallel branches.
Branch ticket/provider-onboarding-links off origin/main; squash-merge.
⚙️ NEW MIGRATION 0041 (additive columns, no new table). Gates: npm test (web vitest + mobile jest) +
npm run test:integration (the providers RLS proofs must stay green — these are additive owner-writable
columns, no policy change). Confirm baselines first.

CONTEXT (read before editing)
- supabase/migrations/0014_providers.sql — providers (bio, logo_url, …). It has NO link columns yet.
- apps/web/src/app/api/providers/route.js (POST) + apps/web/src/app/api/providers/[id]/route.js (PATCH
  profile) — where onboarding/profile fields are written. The PATCH whitelist currently allows
  {name, provider_type, bio, logo_url, slug}; extend it with the link fields.
- apps/web/src/app/provider/ — the web onboarding/profile screens (CreateProviderForm + the Profile
  screen). Add the link inputs there too.
- apps/mobile/src/app/vet-business-access.jsx — the mobile create-business form (now multi-select after
  2.15). Add optional link inputs.

GOAL: a provider can enter their website, Instagram, Facebook, and Google Maps links during onboarding
(and edit them later on the profile), persisted on the providers row. These links are PUBLIC profile data
and are the INPUT that ticket 2.21 (enrichment) reads.

SCOPE
1. Migration 0041 — additive nullable text columns on providers: website_url, instagram_url,
   facebook_url, google_maps_url. No RLS change (providers already: any-authed read, owner|admin write —
   these columns ride that policy). Document: no new table, completeness guard unaffected.
2. Web: extend the providers PATCH whitelist + POST body to accept the four link fields (validate as
   optional strings; light URL sanity check — accept empty/undefined; do NOT hard-fail a messy value, just
   trim). Surface them as inputs on the web onboarding + profile screens. Return them in the provider GET
   so the profile shows them.
3. Mobile: add the four optional link inputs to vet-business-access.jsx (keyboard-safe, correct keyboard
   types/URL), and pass them through useCreateProvider. Keep them OPTIONAL (onboarding stays fast).
4. Surface the links read-only on the public provider profile (mobile provider screen + web public/[slug])
   as tappable links — website/IG/FB open externally, Google Maps opens the map. Empty → just omit (no
   fake links).
5. Tests: web vitest (POST/PATCH persist + return the link fields; bad/empty values don't 500); mobile
   jest (the inputs submit through useCreateProvider); integration: providers RLS proofs green with the
   new columns (owner writes, any-authed reads, another user can't write them).

DO NOT: make links required; add a new table; scrape or fetch anything here (that's 2.21); change
providers RLS; show placeholder/fake links.

VERIFY: npm test (web+mobile) + integration green (baseline + net-new). PR body: the four additive
columns (0041), POST/PATCH + UI on both platforms, public read-only surfacing, RLS unchanged. ⚙️ FLAG FOR
TATS: apply 0041. DEVICE PASS: enter links at onboarding → see them on the profile → they open correctly;
links are optional.
```
