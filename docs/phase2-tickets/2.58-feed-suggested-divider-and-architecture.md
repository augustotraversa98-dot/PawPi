# 2.58 — Feed "Suggested" divider + ARCHITECTURE.md expansion

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Mobile + docs. NO migration.
**Build this LAST in Wave 6** so the ARCHITECTURE.md expansion captures every new table/feature this wave
added (2.51–2.57). Independent of the others otherwise.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile +
web API. Phase-2 ticket 2.58: (1) a "Suggested" divider in the feed and (2) expand ARCHITECTURE.md. Read
docs/phase2-tickets/00-README.md (no-fake-data rule; autonomy preamble). Build LAST in Wave 6.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/feed-suggested-divider-and-architecture.
Gates: npm test (mobile jest). Docs changes need no test.

CONTEXT
- The feed (2.13/2.36/2.37) shows the active pet's + followed pets' posts (the Following group) and may mix
  in suggested/discover content. When suggested content is interleaved without a label it's unclear why a
  post appears. A clear divider separates "from people/pets you follow" from "Suggested for you".
- ARCHITECTURE.md is Code-maintained orientation. After Waves 5–6 it's behind: it should document the new
  surfaces + tables (emergency card 2.51, transport 2.52, vet Rx 2.53, insurance 2.54, plus the Wave 5
  features if not already covered). SCHEMA_NOTES.md tracks migration order — make sure its order line
  reaches the latest applied/known migration.

GOAL
(1) A clean "Suggested" section divider in the feed: followed content first, then a labeled "Suggested for
you" divider before suggested/discover posts (only shown when there IS suggested content — no empty
divider). (2) Expand ARCHITECTURE.md to cover the Wave 6 features + their tables and refresh the migration-
order pointer in SCHEMA_NOTES.md.

SCOPE
- Mobile: insert a labeled divider row between the Following group and the Suggested group in the feed.
  Render the divider ONLY when suggested items exist (never a dangling label over an empty section). Pure
  UI/ordering — don't change what content is fetched beyond grouping/labeling. Keep it on-brand (warm,
  subtle).
- Docs (ARCHITECTURE.md): add concise sections for the Wave 6 features — the emergency card + its public
  tag/link surfaces + the DEFINER public-read functions (2.51), transport on the spine + transport_trips
  (2.52), vet prescriptions/Rx + refill flow (2.53), the insurance capability + plans/leads (2.54), and the
  adoption foster/urgent flags (2.57). Note each new table + its RLS posture (where they live, how access
  is scoped) so the next chat is oriented. Keep it accurate to what merged (read the merged code, don't
  guess).
- Docs (supabase/SCHEMA_NOTES.md): advance the migration-order line to the latest known migration number
  and keep the source-of-truth pointer.
- Tests: mobile jest (the divider renders when suggested content exists; it does NOT render when there's
  none; followed content stays above it). Docs need no test.

DO NOT: introduce fake/suggested data to make the divider appear (label real suggested content only);
restructure the feed fetch; let the divider show over an empty section; write ARCHITECTURE.md from
assumption (document what actually merged).

VERIFY: npm test green. PR body: the conditional Suggested divider + the ARCHITECTURE.md / SCHEMA_NOTES.md
refresh covering Wave 6. DEVICE: the feed shows followed content, then a "Suggested for you" divider before
suggested posts; with no suggested content there's no divider.
```
