# 2.18 — Telehealth (vet video consult)

> ⛔ **DO NOT START THIS TICKET UNTIL 2.15 IS MERGED TO `origin/main`.** Step 1 below appends `telehealth`
> to the mobile onboarding capability multi-select that 2.15 builds (`vet-business-access.jsx`). If 2.15
> isn't in yet, that list is still a single-select and there's nothing to extend — you'd collide or
> rebuild. Verify `origin/main` actually contains 2.15's multi-select before branching; if not, stop and
> wait.

Read `00-README.md` (CORE MODEL — capabilities) + `../phase2-superapp-master-plan.md` (§1 Veterinary row:
"telehealth (later)") first. Depends on: **2.15 (mobile capability multi-select) — HARD PREREQUISITE,
must be merged first**, 2.1 (capabilities), 2.4 (generalized booking), 2.3 (payments, for the consult
fee), 2.5 (chat, reused for pre/post-consult messaging), the consent system (`assertCareAccess` +
`care_access_grants`) and the existing clinical write path. Adds a NEW capability value `telehealth`.
Web + mobile.

```
ORIENTATION
⛔ PREREQUISITE: 2.15 (mobile capability multi-select) MUST already be merged to origin/main — confirm
vet-business-access.jsx is a multi-select before you branch (Step 1 appends to it). If not, STOP and wait.
PawPi web API + Expo mobile. Phase-2 ticket 2.18: TELEHEALTH — a vet VIDEO CONSULT as a new capability.
Read docs/phase2-tickets/00-README.md (CORE MODEL) + master plan §1.
Branch ticket/telehealth off origin/main; squash-merge.
Gates: npm test (web vitest + mobile jest) + npm run test:integration (new table → RLS proofs +
completeness guard). Confirm baselines first.
⚙️ NEW MIGRATION 0040 (one new table + widen two capability CHECKs) — hand-applied to Supabase after merge.
The capability is `telehealth`; gate every provider telehealth route with
requireProviderCapability(providerId,'telehealth').

CONTEXT (read before editing)
- apps/web/src/app/api/utils/providerAuth.js — ALLOWED_CAPABILITIES (add 'telehealth'). This is the app
  mirror of the DB CHECK.
- supabase/migrations/0027_provider_capabilities.sql (provider_capabilities_capability_check, ~L38) AND
  0030_generalized_booking.sql (vet_appointments_capability_check, ~L77) — BOTH list the allowed
  capability set; 0040 must widen BOTH to include 'telehealth' (idempotent drop+recreate, like 0030 did).
- supabase/migrations/0030_generalized_booking.sql — booking is vet_appointments + capability/start_at/
  end_at + booking_status('completed') + the provider_bookings VIEW. A telehealth consult IS a booking
  with capability='telehealth' (start_at/end_at = the slot). REUSE it — do not build a new booking model.
- apps/mobile/src/hooks/useProviders.js — useBookProvider({ providerId, petId, capability, start_at… }).
  Booking a consult = useBookProvider({ capability: 'telehealth' }). Discovery = useProviders ?type=telehealth.
- apps/web/src/app/api/providers/[id]/pets/[petId]/notes/route.js — the EXISTING clinical write (gated
  SOLELY by assertCareAccess scope 'medical_write'). A consult's clinical note goes HERE → vet_notes →
  it already surfaces in the owner's Vet Record. Do NOT create a second notes store.
- supabase/migrations/0031_chat_messaging.sql — message_threads (booking_id → vet_appointments). Pre/post-
  consult coordination reuses this chat. Do NOT build new messaging.
- supabase/migrations/0033_walk_sessions.sql — the PARTICIPANT-SCOPED RLS pattern (owner FOR ALL on their
  pet's session + assigned active provider staff read/write the assigned session, via app_is_active_staff_of).
  telehealth_sessions copies this shape. The session table is the model for live-link privacy.
- The payment layer (2.3) + PaymentsNotConfiguredError pattern — copy the "…not configured → clean 503,
  never crash" behavior for the VIDEO vendor too.

GOAL: an owner discovers a telehealth-capable vet, books a paid video consult, both parties join a video
room at the slot time, and the vet writes a consult note that lands in the pet's Vet Record — all on the
existing spine (booking + payments + chat + consent), with the video vendor scaffolded-but-dormant behind
keys exactly like payments.

SCOPE
1. Capability registration: add 'telehealth' to ALLOWED_CAPABILITIES + widen the two DB CHECKs (0040).
   After this lands, ALSO append telehealth to the mobile onboarding multi-select from ticket 2.15
   (apps/mobile/src/app/vet-business-access.jsx capability list) so a vet can offer it.
2. ⚙️ Migration 0040 — telehealth_sessions: (id, booking_id → vet_appointments(id), pet_id, owner_user_id,
   provider_id, staff_user_id [assigned vet], room_ref [vendor room id/url], status
   scheduled|in_progress|ended|cancelled, started_at, ended_at, summary [short owner-visible recap],
   created_at, updated_at). ENABLE + FORCE RLS, PARTICIPANT-SCOPED (mirror walk_sessions 0033): owner
   FOR ALL on their pet's session; assigned active provider staff read/write ONLY the session they're
   assigned to (app_is_active_staff_of(provider_id)). No third-party read. Harness as-pawpi_app proofs
   (owner sees own; assigned staff sees assigned; another owner / another staff of the same provider see
   ZERO) + the completeness guard classifies it. Also widen the two capability CHECKs here.
3. Video seam (vendor-agnostic, dormant) apps/web/src/app/api/utils/video/index.js:
   getVideoRoom({ session }) → { joinUrl, token? } from a vendor adapter behind env keys (e.g. a Daily/
   Jitsi-style adapter — pick ONE simple seam, keep it swappable; DO NOT hardcode a vendor in the routes).
   Missing keys → VideoNotConfiguredError; the route returns a clean 503 "Video consults aren't set up
   yet" and NOTHING crashes (copy the payments-config pattern). .env.example gets the video keys + a comment.
4. Routes (web):
   - POST /api/providers/[id]/telehealth/sessions — create/ensure the session for a confirmed
     telehealth booking (requireProviderCapability('telehealth') + the booking belongs to the provider).
   - POST .../telehealth/sessions/[sessionId]/join — returns the join link via getVideoRoom() for a
     PARTICIPANT (owner of the pet OR assigned staff — enforced by the session RLS + an explicit check);
     flips status→in_progress + started_at on first join. 503 if video unconfigured.
   - PATCH .../telehealth/sessions/[sessionId] — end (status→ended + ended_at + optional summary), cancel.
   - GET (owner) /api/pets/[id]/telehealth-sessions — the owner's consults for a pet (RLS-scoped).
   The CONSULT NOTE is written through the EXISTING notes route (medical_write grant) — do not duplicate.
5. Owner mobile UX: Services → Telehealth (discovery ?type=telehealth; empty → "No telehealth vets yet",
   no fakes). Book a consult via the generalized booking flow (capability='telehealth', a time slot) → pay
   the consult fee via the existing payment flow (or the clean "payments not configured" message). At the
   slot, a "Join video consult" button → opens the room (or the "video not set up yet" message). After,
   the consult note appears in the pet's Vet Record (already wired via vet_notes). Coordinate over the
   existing chat.
6. Provider web dashboard: telehealth bookings appear in the existing Bookings inbox (capability label);
   a "Join consult" action (getVideoRoom) + write the clinical note via the existing clinical write UI +
   end the consult. No new dashboard shell.
7. Tests:
   - vitest: capability gate (non-telehealth provider → 403); session create/join/end; join returns a
     room link when video configured and a clean 503 when not; participant-only join (a non-participant →
     denied); the consult note path still routes to vet_notes via medical_write (unchanged).
   - integration harness: telehealth_sessions participant-RLS proofs (owner-own, assigned-staff-only,
     outsider-zero) + completeness guard green; the two widened CHECKs accept 'telehealth'.
   - mobile jest: Telehealth discovery list + book(capability='telehealth') + the Join button states
     (configured vs not).

DO NOT: build a new booking model (reuse 2.4 capability='telehealth'); build new messaging (reuse 2.5);
create a second clinical-notes store (write to vet_notes via assertCareAccess 'medical_write'); hardcode a
video vendor or let missing video keys crash (mirror PaymentsNotConfigured → 503); let a non-participant
get a join link; skip RLS/harness proofs on telehealth_sessions; surface fake consults. Capability ≠ data
access — the video session is participant-scoped, but reading/writing the pet's medical record still
requires the consent grant.

VERIFY: npm test (web + mobile) + npm run test:integration green (baseline + net-new). PR body: new
capability + the two widened CHECKs, telehealth_sessions + participant RLS proofs, the dormant video seam
(503 when unconfigured), booking/chat/notes/payments all REUSED (not rebuilt), migration 0040. ⚙️ FLAG FOR
TATS: apply 0040; set the video vendor env keys when ready (until then consults book + the join shows a
clean "not set up yet"). DEVICE PASS (docs/test-backlog.md): discover a telehealth vet → book → pay-or-
clean-message → Join states → consult note appears in Vet Record → participant-only privacy.
```
