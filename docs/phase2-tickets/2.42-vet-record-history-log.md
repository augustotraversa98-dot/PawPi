# 2.42 — Vet Record: append-only dated history log (vets add, can't delete; owner can)

Read `00-README.md` (Wave 5 autonomy preamble) first. Web + mobile. Covers #8. Build AFTER 2.41 (same Vet
Record area). `vet_notes` already exists with provider-INSERT / owner-only-UPDATE+DELETE RLS (R2d) — the
integrity model is mostly already enforced; this surfaces it + fills gaps.

```
ORIENTATION
PawPi web (provider) + Expo mobile (owner) + API. Phase-2 ticket 2.42: a dated, APPEND-ONLY clinical
history log on the pet's record. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; if an integrity edge case is ambiguous, choose the most data-preserving option
(append, never destroy) and proceed. CI green → auto squash-merge; red → fix then merge. Keep going
through Wave 5. One branch off origin/main.
Branch ticket/vet-record-history-log. Gates: npm test (web vitest + mobile jest); npm run test:integration
(the integrity rules are RLS — prove them).

CONTEXT
- vet_notes (RLS R2d migration 0023): provider active-staff INSERT via an active medical_write grant;
  UPDATE/DELETE are OWNER-ONLY. So providers ALREADY cannot delete/alter notes — the integrity model is
  in place. Provider clinical write route: apps/web/src/app/api/providers/[id]/pets/[petId]/notes.
- Owner Vet Record UI: apps/mobile/src/components/Health/HealthVetRecord.jsx + VetRecord/*.
- Provider web dashboard clinical screens (the record view + write-note).

GOAL: the pet's record shows a chronological history log — each entry stamped with its created date +
author (which clinic/vet, or the owner) — plus a running general summary at the top. Vets can ADD entries
(append) and never delete or rewrite older ones; only the OWNER can delete entries. History is preserved.

SCOPE
- History log view (owner mobile + provider web): list every clinical entry newest-first with created_at
  + author label (provider/clinic name or "You"), reusing vet_notes (+ owner-authored entries — see
  below). A "General summary" block at the top: simplest correct option = the most recent owner/vet-
  maintained summary OR a short derived recap; pick one and keep it real (no fake). (The richer AI summary
  is ticket 2.50 — don't duplicate that here.)
- Append-only enforcement: vets INSERT new dated entries only; they cannot edit/delete any entry (already
  true via RLS — confirm + assert in tests; do NOT add a provider UPDATE/DELETE policy). The OWNER can
  delete entries.
- Owner-authored entries: if the owner should also add notes to the same log and vet_notes has no owner-
  author path, add minimal support (e.g. an author_user_id / source column) — migration ONLY if needed,
  flagged for Tats; otherwise reuse what's there. Keep RLS correct (owner writes own; providers can't
  touch owner entries or older entries).
- Tests: integration RLS — a provider with medical_write can INSERT but a provider DELETE/UPDATE of any
  entry returns zero/denied; the owner can delete; another provider/user sees nothing. web/mobile render
  of the dated log + summary + empty state.

DO NOT: allow providers to delete or rewrite history (the whole point); show fake entries; duplicate the
2.50 AI summary; add a migration unless owner-authored entries genuinely require a column.

VERIFY: npm test + integration green. PR body: append-only proof (provider can't delete/edit, owner can),
the dated log + summary. ⚙️ Flag any migration in test-backlog ACTION 1.
```
