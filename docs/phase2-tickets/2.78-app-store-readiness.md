# 2.78 — App Store readiness: debugging + compliance hardening pass (DO LAST in the autonomous run)

Read `00-README.md` (shared conventions + the ⚡ autonomy preamble) first. Cross-cutting hardening pass.
Goal: make PawPi **ready to submit to the App Store** — find and fix every safe code/config issue measured
against **Apple's current App Store Review Guidelines + iOS submission requirements**, and produce a clear
readiness report for the human-only / account-gated steps. Run this AFTER all of Wave 7 (2.68–2.75) is
merged.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo app (SDK 54,
RN 0.81.5, expo-router 6; iOS target) + web API. Final-step ticket 2.78: an App Store readiness + debugging
pass. Read docs/phase2-tickets/00-README.md (shared conventions; the autonomy preamble applies).
AUTONOMY: I'm away — do NOT ask. Auto-FIX every clear, safe code/config issue and merge when CI is green;
for anything that's a POLICY judgment, a business/legal decision, or needs the Apple Developer account,
do NOT guess or rip things out — record it in the readiness report and keep going. Branch(es) off
origin/main; squash-merge each when green. May span MULTIPLE PRs (e.g. account-deletion is sizable) — keep
each PR coherent and green.
Gates: npm test (web vitest + mobile jest) + npm run test:integration (RLS) stay green; add tests for any
new behavior (e.g. account deletion).
FIRST: re-read the CURRENT Apple App Store Review Guidelines + the iOS app submission/privacy requirements
(they change — verify against the live guidelines, don't rely on memory). Map findings to guideline numbers
in the report.

GOAL
A codebase that passes a realistic pre-submission review: tests/lint/types green; all required iOS privacy
+ permission declarations present and honest; in-app account deletion; Sign in with Apple parity; graceful
permission/error/offline handling; no fake data, dead ends, debug surfaces, or placeholder screens; basic
accessibility; sane app metadata — plus a docs/app-store-readiness.md that lists what was fixed and the
remaining account-gated submission checklist.

SCOPE — AUTO-FIX (do these; they're unambiguously "good fixes")
- Green the project: fix any failing/flaky tests, lint errors, and type errors across web + mobile. Remove
  the known `wrongPets` debug query in pets/route.js (flagged in test-backlog) and any other debug-only code
  paths, console error surfacing to users, or dev banners.
- iOS permission usage strings (app.json → expo ios.infoPlist): provide clear, honest NS*UsageDescription
  for every capability the app actually uses — location (when-in-use; "always"/background only if live
  walk/transport GPS needs it), camera, photo library (read + add), microphone (if telehealth video),
  notifications. Do NOT declare permissions the app doesn't use. If the app does NOT track users across
  apps, do not add ATT/NSUserTrackingUsageDescription (and don't add the tracking framework).
- iOS Privacy Manifest (PrivacyInfo.xcprivacy via the Expo config): declare collected data types + their
  purposes and the "required reason" APIs the app/its libs use (e.g. UserDefaults, file timestamps). Make it
  match what the app actually does.
- In-app ACCOUNT DELETION (Guideline 5.1.1(v) — REQUIRED for apps with account creation; PawPi has accounts).
  Build it if missing: Settings → "Delete account" → clear confirmation → a web API route that deletes (or
  irreversibly anonymizes) the caller's account + owner-scoped data (respecting RLS, cascading/soft-deleting
  pets/health/posts/etc. the owner owns; handle provider-staff/owned-provider edge cases sensibly), then
  signs the user out. Owner-scoped + harness-proven; add tests. This is distinct from "Reset App Data".
- Sign in with Apple parity (Guideline 4.8 / 5.1.1): the app offers Google sign-in (2.46) → Apple must be
  offered too and presented equivalently. Verify it's present + not hidden when Google shows; fix ordering/
  visibility if off. (Keep the env-gating: buttons stay "Coming soon" until keys are set — that's fine
  pre-launch; ensure the COMPLIANCE structure is correct.)
- Graceful permission denial + error/offline handling everywhere (location, camera, photos, notifications,
  network): no crashes, no dead screens — clear messages + fallbacks. Add error boundaries on screens and
  null-safety on the new Wave 7 surfaces (places/events/transport-track/rx/insurance/nutrition).
- Functional completeness (Guideline 2.1 / 2.3.1): no broken links, no-op buttons, "coming soon" stubs that
  look live, or placeholder/lorem content; every shipped feature works or is clearly gated. Keep the no-fake-
  data rule — real data or honest empty states only.
- Accessibility basics: accessibilityLabel/role on icon-only controls; ensure Dynamic Type doesn't break
  core layouts; check obvious contrast issues. (Deep visual a11y belongs to 2.77 — do the functional pass.)
- App metadata sanity (app.json): version + iOS buildNumber set; app icon + splash present; bundle
  identifier present (a placeholder is fine until the Apple account — flag it); supported orientations; min
  iOS version reasonable for the APIs used.
- Medical positioning (Guideline 1.4.1): confirm the non-diagnostic disclaimers are present on health/
  nutrition/vet-summary surfaces (project rule already).

SCOPE — FLAG ONLY (do NOT unilaterally decide; document in docs/app-store-readiness.md)
- Payments / IAP (Guideline 3.1): map every paid flow and confirm it's a REAL-WORLD service / physical good
  (vet, grooming, walking, daycare, sitting, training-as-service, transport, adoption fees, shop physical
  products, insurance) → external payment (MercadoPago/Binance) is permitted; flag if ANY digital-only
  content is sold via external payment (that would require IAP). Document the mapping + any risk; do NOT
  remove payment rails.
- Privacy policy + terms URLs (required for submission): if not wired, add a clearly-marked config slot +
  flag for Tats to supply real URLs.
- Account-gated submission steps (need the Apple Developer account / EAS build / App Store Connect): real
  bundle ID + signing/provisioning, capabilities, App Store Connect listing, privacy "nutrition labels",
  screenshots, age rating, export-compliance, EAS build + TestFlight/upload. List as a tick-box checklist.

DELIVERABLE
- docs/app-store-readiness.md: (1) FIXED — each issue + the guideline # + what changed; (2) FLAGGED —
  policy/legal items (payments mapping, privacy/terms URLs) for Tats; (3) ACCOUNT-GATED CHECKLIST — the
  submission steps that need the Apple account/EAS, in order. This is the pre-submission handoff.

DO NOT: ask questions or stall on a doubt (auto-fix the clear stuff, flag the rest, keep going); remove
payment rails or make a policy/legal call unilaterally; fabricate privacy/terms content; auto-apply
migrations to Supabase (flag any new one in test-backlog ACTION 1 — e.g. an account-deletion helper);
introduce fake data; merge red CI; touch the native widgets track (2.76) or the 2.77 redesign (separately
gated). Leave the tree clean if you hit the token limit.

VERIFY: npm test + integration green; completeness guard green (if any table/helper added for account
deletion); the readiness report exists and is accurate. PR body(s): the fixes by guideline #, the new
account-deletion flow (+ RLS proof), and a pointer to docs/app-store-readiness.md for the flagged + account-
gated items. NOTE: actual archive/upload to the App Store needs the Apple Developer account + an EAS build —
this ticket gets the CODE + CONFIG submission-ready and documents the rest; it does not (cannot) upload.
```
