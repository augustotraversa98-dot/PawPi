# 2.55 — Remove hardcoded "Phoebe" strings + proper avatar fallback

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Mobile (maybe a touch of web).
NO migration. Tiny quality/cleanup ticket — good momentum starter for the wave. Independent, parallel-safe.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile +
web API. Phase-2 ticket 2.55: remove leftover hardcoded demo strings (notably "Phoebe") and give every
avatar a proper fallback. Read docs/phase2-tickets/00-README.md (no-fake-data rule; autonomy preamble).
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/remove-phoebe-avatar-fallback.
Gates: npm test (web vitest + mobile jest).

CONTEXT
- The no-fake-data rule (PawPi_instructions.md): no placeholder names, counts, or sample images in
  production — empty states only. A hardcoded "Phoebe" (and any sibling demo strings) violates it and
  must be replaced by real data or a neutral fallback.
- Avatars currently can render blank/broken when a pet or user has no photo.

GOAL
(1) Find and remove EVERY hardcoded "Phoebe" (and any other obvious demo/sample literal it surfaces while
searching — e.g. sample dog names, fake handles, placeholder owner names) used as display data; replace
with the real value from the data layer, or a neutral fallback when truly absent. (2) Give avatars a
consistent fallback: when there's no photo, show initials (or a paw glyph) on the brand color — never a
broken image, never a hardcoded sample photo.

SCOPE
- grep the mobile (and web) tree for "Phoebe" and obvious demo literals; for each, either wire it to the
  real source (pet/user/profile data already available) or render the correct empty/fallback. Do NOT
  invent data.
- A small shared Avatar fallback: a component/util that, given a name + optional photo URL, renders the
  photo if present else initials/glyph on the brand color. Reuse it wherever pet/user avatars render
  (feed, profile, messages, vet record, etc.) — start with the spots that currently break.
- Keep it surgical: this is cleanup, not a redesign. Don't change layouts beyond the avatar fallback and
  the string fixes.
- Tests: mobile jest (no "Phoebe"/demo literal renders where real data exists; avatar with a URL shows
  the image, without a URL shows initials/glyph not a broken image). A guard test that greps the rendered
  output / component for the literal is welcome.

DO NOT: introduce any new fake/sample data; redesign screens; leave a broken-image avatar; remove a string
that is actually a legitimate i18n key or test fixture (only display-data literals). 

VERIFY: npm test green. PR body: the list of removed hardcoded literals (where each was + what replaced it)
and the shared avatar fallback + where it's now used. DEVICE: no "Phoebe" anywhere; a pet/user with no
photo shows a clean initials/glyph avatar; one with a photo shows it.
```
