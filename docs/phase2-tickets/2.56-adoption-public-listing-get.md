# 2.56 — Adoption: public single-listing GET (close the 2.30 deviation)

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Web (+ a small mobile simplification).
NO migration. Closes the documented deviation from ticket 2.30. Independent, parallel-safe.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile +
web API. Phase-2 ticket 2.56: add a real PUBLIC single-listing GET for an adoptable dog, replacing the
2.30 workaround. Read docs/phase2-tickets/00-README.md (shared conventions; autonomy preamble).
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/adoption-public-listing-get.
Gates: npm test (web vitest + mobile jest); npm run test:integration if you touch RLS.

CONTEXT
- 2.30 (adoption per-listing deep-link) shipped with a deviation: the single-listing route
  (adoptable-listings/[listingId]) was ADMIN-ONLY (no public GET), so the feed deep-link works around it
  by loading the place's PUBLIC list (available-of-published) and FINDING the dog there. That works but is
  wasteful and fragile (won't resolve a listing that isn't in the currently-loaded public list).
- The adoption listings table already has published/available + RLS (public read of published-available
  rows). We just need a clean public read endpoint for ONE listing.

GOAL
Add a public GET for a single adoptable listing that returns the listing IFF it is published AND available
(the same visibility the public browse uses) — exposing only public fields (story, vitals, photos, fee/
donation, provider/place). Then simplify the 2.30 mobile deep-open to call it directly, with the graceful
"no longer available" path preserved for unpublished/adopted/removed listings.

SCOPE
- Web: a public GET adoptable-listings/[listingId] (or the public providers/.../listings/[id] path that
  fits the existing routing) that returns the single listing only when published + available, public
  fields ONLY (no admin/applicant/owner-private data), 404/"not available" otherwise. Reuse the existing
  visibility predicate the public browse uses — do NOT loosen RLS. If RLS already permits published-
  available single-row reads, this is purely a route; if not, ensure the read path stays within the
  existing public policy (no new broad policy).
- Mobile: change the 2.30 deep-open to fetch the single listing directly via the new endpoint; keep the
  "no longer available" notice when it returns nothing; keep routing through the root-level service/ stack
  (the 2.19 nav fix) so the More tab is never corrupted and back returns to the feed.
- Tests: web vitest (published+available → returns public fields; unpublished/unavailable → not returned;
  no private fields leak); integration only if you touched a policy (otherwise none — reuse the proven
  public-read); mobile jest (deep-open fetches the single listing; missing → graceful notice, no modal).

DO NOT: expose unpublished/adopted/private listing data or applicant info; loosen adoption RLS; change the
nav behavior from 2.19/2.30; show a fake dog. 

VERIFY: npm test (+ integration if a policy changed) green. PR body: the new public single-listing GET
(published+available, public fields only) + the simplified 2.30 deep-open. DEVICE: tap an Adopt-me card in
the feed → that exact dog's detail opens via the direct fetch (even one not in the current browse list);
an adopted/removed dog → a clean "no longer available" notice; back returns to the feed, More stays clean.
```
