# 2.66 — Health → Today → "Today's Progress" on REAL logged data (no fake data)

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Mobile (+ web aggregation if needed).
NO migration. Removes hardcoded fake stats. High-priority no-fake-data fix.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile + web
API. Phase-2 ticket 2.66: the "Today's Progress" card on Health → Today shows HARDCODED FAKE stats — make
it reflect what was actually logged today. Read docs/phase2-tickets/00-README.md (the NO-FAKE-DATA rule is
a core project principle; autonomy preamble).
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/health-today-real-progress.
Gates: npm test (web vitest + mobile jest); npm run test:integration if you touch RLS.

CONTEXT
- components/Health/HealthToday.jsx renders a "Today's Progress" card (~line 1134) with HARDCODED chips:
  "🍽️ Fed 2 times", "🚶 1 walk", "💊 Medication given". This is fake data and violates the project's
  no-fake-data rule (PawPi_instructions.md) — it must reflect the pet's REAL completions for today.
- Health → Today already generates reminders from routines (Feeding / Walks / Photo Check / Medical Care /
  Wellness / Vet Appointment) and logs completions ("X logged"). Completing one item logs it; history is
  preserved. The progress card should summarize TODAY's real completions for the ACTIVE pet, scoped by
  pet_id/owner_user_id, against the phone's local day.
- Use whatever the app already records as "logged today" (the same source the Today list marks done /
  the health logs) — do not invent a new logging mechanism.

GOAL
"Today's Progress" reflects the active pet's REAL activity today: e.g. "🍽️ 1/3 meals logged", "🚶 2 walks
done", "💊 Medication given" — derived from actual logged completions, scoped to the current pet, using the
local day. When nothing has been logged yet, show a clean empty/encouraging state (NOT fake numbers). The
counts update as the user logs items during the day.

SCOPE
- Replace the hardcoded chips with values computed from real data. Prefer counts WITH the day's target
  where a target exists (e.g. meals logged / meals scheduled today = "1/3 meals"); for activities without a
  fixed target, show the done count ("2 walks done"). Cover at least feeding, walks, medication, and any
  other category the Today list tracks; render a chip only for categories that have a schedule/target or a
  logged event today (no chip for irrelevant categories).
- Source the data from the existing completion/log source for the active pet + today (local day). If a
  small read/aggregation route is cleaner than client-side counting, add one (web), owner+pet-scoped,
  reusing existing RLS (no new table, no migration). Otherwise compute from the data the Today screen
  already loads.
- Empty state: before anything is logged, show an encouraging empty state (e.g. "Nothing logged yet
  today") — never fake counts.
- Keep it non-diagnostic and consistent with the warm design; counts must match what the Today list shows
  as done.
- Tests: web vitest (if an aggregation route is added: returns real per-category counts/targets owner+pet
  scoped, empty when nothing logged); integration only if a new policy (else reuse proven reads); mobile
  jest (the progress card renders REAL counts from provided data incl. the n/target form; updates when a
  log is added; empty state when nothing logged; NO hardcoded "Fed 2 times").

DO NOT: keep ANY hardcoded/sample numbers; count another pet's or another day's activity (scope to active
pet + local today); add a migration or new table; show fake counts in the empty state; diverge from what
the Today list marks as done.

VERIFY: npm test (+ integration if a route/policy added) green. PR body: the real-data Today's Progress
(counts vs targets where applicable), the data source, the empty state — and confirmation the hardcoded
chips are gone. DEVICE: with nothing logged → an empty/encouraging state (no fake numbers); log a meal →
"meals" increments (e.g. 1/3); complete a walk → walks count rises; the chips match the Today list; another
pet/day isn't counted.
```
