# 2.48 — Lost & Found (lost mode + local alerts + sightings)

Read `00-README.md` (Wave 5 autonomy preamble) first. Web + mobile. EPIC feature E3. New tables →
migration. Uses location (reuse the 2.43 map work if merged).

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.48: a Lost & Found network — an owner flips a pet to LOST,
nearby users get alerted, and anyone can report a sighting. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible model and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5. One branch off origin/main.
Branch ticket/lost-and-found. Gates: npm test (web vitest + mobile jest); npm run test:integration (RLS).

CONTEXT
- Pets + their real profile (photo/name/breed) already exist. Map/location handling: reuse the approach
  from 2.43 (lat/lng + bounding-box nearby search; no PostGIS needed). Notifications: the in-app
  notifications table (0044) + app_notify DEFINER helper from 2.26 — extend its type set for a lost alert.
- Emergency/contact + microchip live on the pet medical profile (Vet Record).

GOAL: an owner activates "Lost mode" for a pet with last-seen location + notes; nearby users (and ideally
the owner's followers/local community) get a lost alert with the pet's photo/name/area + microchip note;
anyone can submit a SIGHTING (location + note + optional photo) that routes back to the owner; the owner
resolves it (found) which clears lost mode.

SCOPE
- Migration (next free ≥0046): lost_reports (id, pet_id, owner_user_id, status ['active','resolved'],
  last_seen_lat, last_seen_lng, last_seen_area text, notes, reward text nullable, created_at,
  resolved_at) + lost_sightings (id, lost_report_id, reporter_user_id nullable, lat, lng, note,
  photo_url, created_at). ENABLE+FORCE RLS: an ACTIVE lost_report is readable by any authed user (it's a
  public alert), writable/resolvable by the owner; a sighting is insertable by any authed user, readable
  by the report owner (+ the reporter their own); the owner resolves. Harness proofs (owner manages;
  anyone can read an active alert + report a sighting; only owner resolves; non-owners can't edit the
  report). Also extend the 0044 notifications type CHECK to include a 'lost_alert' type (additive). Flag
  migrations in test-backlog ACTION 1.
- Alerting: on activation, notify relevant users via app_notify — at minimum the owner's followers; if
  feasible, users near the last-seen location (bounding-box over recent pet/owner locations you already
  have, else followers + community). Keep it best-effort (a notify failure never blocks activation).
- Mobile: a "Mark as lost" action on the pet (with last-seen map pin + notes + optional reward); a Lost &
  Found browse/near-me list; a sighting-report form; the owner's incoming-sightings view; a "Found /
  resolve" action. A lost pet is clearly badged in the app.
- Web: read route(s) as needed; this is primarily mobile/owner-facing.
- Tests: web vitest (activate/resolve owner-only; sighting insert by anyone; owner reads sightings);
  integration (active alert any-authed read, sighting RLS, owner-only resolve, notifications type widened);
  mobile jest (mark lost, browse near-me, report sighting, resolve).

DO NOT: expose a resolved/owner's private data beyond the alert essentials; let non-owners resolve or edit
a report; block activation if notify fails; show fake lost pets. Migrations hand-applied by Tats — flag.

VERIFY: npm test + integration green. PR body: lost_reports/lost_sightings + RLS proofs, the alert via
app_notify (type widened), the near-me browse + sighting + resolve flow. DEVICE: mark a pet lost → another
account sees the alert + reports a sighting → owner sees it → resolve clears lost mode.
```
