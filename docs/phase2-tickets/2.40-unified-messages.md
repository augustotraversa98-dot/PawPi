# 2.40 — Unified Messages: search owners + businesses in one place (tabs)

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + small web read. Covers #10. Builds on the
real DM system (2.27) + provider chat (2.5) — both already exist. No migration expected.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.40: one Messages hub (feed area) that holds BOTH owner↔owner
DMs and owner↔business conversations, with a way to start a chat by SEARCHING for a pet owner. Read
docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible UX and proceed. CI green → auto squash-merge; red → fix then merge.
Keep going through Wave 5. One branch off origin/main. NO migration (reuse existing tables/routes).
Branch ticket/unified-messages. Gates: npm test (web vitest + mobile jest).

CONTEXT
- Owner↔owner DMs (2.27): apps/mobile/src/app/messages.jsx + chat.jsx; dm_threads/dm_messages + routes.
- Owner↔provider chat (2.5): apps/mobile/src/app/provider-messages.jsx + provider-chat.jsx;
  message_threads/messages + routes. Today the provider conversation is reached only from INSIDE each
  provider — there's no central place to find it.
- User search exists from 2.25 (search pets/owners). Reuse it to find an owner to DM.

GOAL: the feed Messages screen shows ALL conversations — owner↔owner AND owner↔business — with a
tab/filter to switch between "People" and "Businesses". A search lets you find a pet owner and start a
new DM. The per-provider "Message" button stays, but the resulting conversation is now stored in and
findable from this central Messages hub.

SCOPE
- Messages hub: list both DM threads (people) and provider threads (businesses) together, most-recent
  first, each labeled with the counterpart's name/avatar + last message + unread badge. Add a
  tab/segmented filter: All / People / Businesses (pick the clearest; recommended All + People +
  Businesses).
- Start-a-DM: a search field (reuse the 2.25 owner search) → tap an owner → open/create the DM thread
  (the 2.27 create-thread route is idempotent per pair).
- Provider entry: keep the "Message" button on provider profiles; tapping it opens/creates the provider
  thread (existing) — and that thread now appears in the central Businesses filter too.
- Tab routing into the correct conversation view (chat.jsx for DMs, provider-chat.jsx for businesses) —
  reuse both; do not merge the two backends.
- Tests: web vitest (the owner-search endpoint returns owners; any new combined-list read is scoped
  correctly); mobile jest (the hub lists both kinds + filters; search starts a DM; provider button opens
  the business thread; unread badges).

DO NOT: merge the two messaging backends (DM vs provider chat stay separate tables/routes — just present
them together); add a migration; show fake conversations; break the provider-side chat.

VERIFY: npm test green. DEVICE: Messages shows people + businesses with a filter; search→start a DM works;
a provider "Message" conversation appears in Messages afterward.
```
