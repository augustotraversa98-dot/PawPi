# 2.43 — Schedule walks with buddies (map + public/private + invites)

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + web API. Covers #5. EXTENDS the existing
`social_walks` (0007) — likely a migration to add map coordinates.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.43: let owners schedule social WALK EVENTS on a map —
private (invite specific users) or public (anyone searching for a walk in the area can find it), for pets
to socialize. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option (incl. the map library) and proceed. CI green → auto squash-
merge; red → fix then merge. Keep going through Wave 5. One branch off origin/main.
Branch ticket/walks-with-buddies. Gates: npm test (web vitest + mobile jest); npm run test:integration
(RLS).

CONTEXT (the foundation already exists — extend, don't rebuild)
- supabase/migrations/0007_social_walks.sql — social_walks (scheduled_at, visibility
  ['private','friends_only','nearby_pets'], meeting_location_details, status) + social_walk_join_requests
  (RLS R2g: read=own/approved/walk-owner, INSERT requester, UPDATE walk-owner). NO lat/lng yet.
- The web app already depends on @vis.gl/react-google-maps. For mobile, use the available map lib or add
  react-native-maps (Expo-supported) — pick one and proceed.

GOAL: an owner creates a walk event with a date/time and a LOCATION pinned on a map, choosing PUBLIC
(discoverable by anyone searching walks in that area) or PRIVATE (visible only to specifically invited
users). Others can find public walks near them, see details, and request to join / RSVP.

SCOPE
- Migration (next free number ≥0046): add to social_walks: lat numeric, lng numeric, location_name text
  (+ keep meeting_location_details). Map "public" onto the existing 'nearby_pets' value (area-discoverable)
  and "private" onto 'private' (invite-only) — reuse the existing visibility CHECK; if you need a literal
  'public', widen the CHECK additively. For private invites, reuse social_walk_join_requests OR add a
  walk_invites table (pick the simpler that fits the routes). RLS for any new table: a private walk is
  visible only to its owner + invited users; a public walk is visible to any authed user; writes
  owner-scoped. Harness proofs + completeness guard. Flag the migration in test-backlog ACTION 1.
- Create flow (mobile): date/time + a MAP picker to drop the meeting pin (lat/lng + a place label) +
  public/private toggle + (if private) pick users to invite (reuse owner search from 2.25).
- Discovery (mobile + web read): list/search PUBLIC walks near a location (simple radius/bounding-box on
  lat/lng — keep it simple, no PostGIS required) with date/time + map preview. Private walks never appear
  in discovery to non-invitees.
- Join/RSVP: reuse social_walk_join_requests (request → owner approves) and surface attendees.
- Tests: web vitest (create with coords; nearby search returns public-only + excludes private; invite +
  join flow); integration (private walk invisible to non-invitees, public visible to any authed, RLS
  proofs); mobile jest (map picker sets coords; public/private toggle; discovery list; RSVP).

DO NOT: expose a private walk to non-invitees; rebuild social_walks (extend it); require PostGIS (a
bounding-box filter is fine); show fake walks. Migration is hand-applied by Tats — flag it, don't auto-apply.

VERIFY: npm test + integration green. PR body: the coords migration, public/private model, map picker,
nearby discovery, invites/RSVP, RLS proofs. DEVICE: create a public walk on the map → another account
finds it nearby; a private walk only shows to invitees.
```
