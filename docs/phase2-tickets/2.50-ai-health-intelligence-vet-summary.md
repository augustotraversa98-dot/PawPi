# 2.50 — AI health intelligence + real Vet Summary

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + web API. EPIC feature E1 — the moat. Build
AFTER 2.41 + 2.42 (vet record must be real first). The single biggest ticket — do it LAST. Replaces the
fully-fake Vet Summary and adds a real insights layer. Likely no migration (read-only aggregation; an
optional cache table only if needed).

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.50: turn PawPi's longitudinal health data into INSIGHT — a
real, date-ranged Vet Summary + pattern/trend intelligence that helps owners prepare better vet
conversations. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick sensible thresholds/designs and proceed. CI green → auto squash-merge; red → fix
then merge. This is the last Wave-5 ticket — if tokens run low, ship the Vet Summary rebuild first (it's
the higher-priority, queued item) then the insights. One branch off origin/main.
Branch ticket/ai-health-intelligence. Gates: npm test (web vitest + mobile jest); npm run test:integration
only if you add a cache table.

POSITIONING (non-negotiable, from the project rules)
- The app does NOT diagnose. Everything here is framed as "track changes + prepare better vet
  conversations." Keep the disclaimer: "Social Pet helps you track changes and prepare better
  conversations with your veterinarian. It does not diagnose or replace professional veterinary care."

PART A — REAL VET SUMMARY (priority; this is the queued rebuild)
- apps/mobile/.../VetSummary/VetSummaryModal.jsx is CURRENTLY 100% FAKE — it renders a hardcoded
  mockSummaryData object, takes no petId, fetches nothing, and the "Download PDF" + "Share with Vet"
  buttons are dead. This violates the no-fake-data rule + priority #2. REBUILD it.
- NOTE: the existing /api/vet-record/summary returns only COUNTS for the dashboard — it is NOT the summary
  content. Build a NEW date-ranged aggregation route that pulls ALL of a pet's health data for a chosen
  range, scoped by pet_id + owner_user_id (RLS): food/water logs, poo/pee/vomit, weight, wellness logs,
  medical-care logs, photo checks (by body area), walks/activity, allergies/conditions/current meds, vet
  notes/history (2.42), vaccinations. Rewire the modal to real data for a chosen date range; keep the
  user-added "questions for the vet"; wire Download PDF (e.g. expo-print → PDF) + Share (system share);
  keep the disclaimer.

PART B — HEALTH INTELLIGENCE (the moat)
- Pattern/trend detection over the same longitudinal data, per active pet: e.g. notable weight trend,
  water/appetite intake drift, increasing soft-stool/vomit frequency, photo-check changes over time,
  missed-medication adherence. Implement as PURE, TESTED, RULE/STATISTICAL helpers (thresholds over a
  window) — NO LLM needed for the trends. Surface as gentle "worth mentioning to your vet" flags on
  Health → Insights (and feed the Vet Summary). NEVER diagnostic language.
- OPTIONAL natural-language recap: if an LLM key exists (reuse the 2.21 ENRICHMENT_LLM_KEY pattern,
  env-gated), generate a friendly plain-language summary of the flags; if no key, fall back to the
  rule-based text cleanly (no crash, no "not configured" dead-end — the rule-based insights always work).
- Photo-check trends: a side-by-side/over-time view per body area (reuse existing health_photo_checks).
- Optionally light breed/age-stage context (puppy vs senior) as curated, non-diagnostic guidance.

SCOPE / TESTS
- New date-ranged aggregation route (owner+pet scoped; RLS-correct; only the owner's data). Pure
  aggregation + the trend helpers fully unit-tested (thresholds, windows, empty-safe — thin data → "not
  enough data yet", never invented findings). Vet Summary modal rewired to real data + PDF + share. mobile
  jest for the rewired modal + insights cards; web vitest for the aggregation route (scoping + shape);
  integration only if you add a cache table (prefer none).

DO NOT: keep ANY mock summary data; produce diagnoses or alarming claims (use "worth discussing with your
vet"); fabricate findings on sparse data; hard-fail when the LLM key is absent (rule-based path is the
baseline); leak another pet/owner's data. Migration only if a cache table is truly warranted — flag it.

VERIFY: npm test (+ integration if cache added) green. PR body: the real date-ranged Vet Summary
(aggregation + PDF + share, mock gone), the tested rule-based insight flags, optional env-gated LLM recap
with clean fallback, photo-trend view, disclaimer intact. DEVICE: open Vet Summary for a real pet+range →
real data, export/share works; Insights shows real "discuss with your vet" flags; sparse data → graceful
empty.
```
