# 2.36 — Feed daily-post fixes (delete/reupload, view-today, own post in feed)

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + web API. Covers list items #11.4, #11.5,
#11.6. Likely adds a post DELETE route (no migration — posts RLS already owner-write).

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.36: fix the daily-post pipeline. Read
docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5. One branch off origin/main.
Branch ticket/feed-daily-post-fixes. Gates: npm test (web vitest + mobile jest); npm run test:integration
only if you touch a DB/RLS path.

THREE BUGS
1. (#11.4) You can't DELETE the daily photo you posted. Add delete + allow re-upload for the same day.
2. (#11.5) "View today's post" doesn't show the post — tapping it shows nothing.
3. (#11.6) Your OWN latest post doesn't appear in the feed; only friends' posts show, in posting order.

CONTEXT
- apps/web/src/app/api/posts/route.js (feed: Following-first then Suggested), posts/[id] (has paw/barks
  subroutes but NO DELETE yet), posts/owner-posted-today + posts/today-daily-update (the BeReal daily
  lock + "today's post").
- apps/mobile/src/components/Feed/* — PostCard, PostComposerModal, DailyPromptCard, PostDetailModal;
  hooks/useFeedPosts.js, useFeedData.js.
- posts RLS: owner can write/delete their own (owner_user_id). BeReal lock keys off owner-posted-today.

GOAL: an owner can delete today's daily post and post a new one; "view today's post" reliably opens the
real post; and the owner's own posts appear in their feed in chronological order alongside followed pets'.

SCOPE
1. Delete + reupload: add DELETE /api/posts/[id] (owner-only; soft or hard delete — pick soft via
   deleted_at if a column exists, else hard; document). Deleting TODAY's daily must reset the BeReal lock
   (owner-posted-today returns false again) so the composer reopens and the user can re-upload. Wire a
   delete affordance on the user's own daily (PostCard / PostDetailModal) with a confirm.
2. View-today: fix "view today's post" so it opens the actual post (today-daily-update / DailyPromptCard /
   PostDetailModal path). Diagnose why it shows nothing (wrong id, missing fetch, modal not opening) and
   fix.
3. Own post in feed: include the viewer's own posts in the feed result, ordered chronologically with the
   rest (don't exclude self). Confirm it actually persists + appears right after posting (the "looks like
   it didn't save" symptom — verify the create→refetch path inserts it into the list).
- Tests: web vitest (DELETE owner-only + resets the daily lock; feed includes own posts in order); mobile
  jest (delete affordance + confirm; view-today opens the post; own post renders in the feed).

DO NOT: break the BeReal daily lock for the normal case; let a user delete someone else's post; show fake
data; add a migration unless a soft-delete column is genuinely needed (prefer reusing existing).

VERIFY: npm test (+ integration if DB touched) green. DEVICE: delete today's daily → repost works; "view
today's post" shows it; your own posts show in the feed in order.
```
