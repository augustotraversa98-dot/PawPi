# 2.46 — Sign in with Apple + Google

Read `00-README.md` (Wave 5 autonomy preamble) first. Web auth + mobile. Covers #1 + #2. HIGH BLAST
RADIUS (auth) — additive only, must never break existing login.

```
ORIENTATION
PawPi web auth (Auth.js) + Expo mobile. Phase-2 ticket 2.46: add Sign in with Apple AND Sign in with
Google as ADDITIONAL providers, without disturbing the existing email/password login. Read
docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the standard Auth.js provider setup and proceed. CI green → auto squash-merge;
red → fix then merge. Keep going through Wave 5. One branch off origin/main.
IMPORTANT (unattended-merge safety): this touches auth. Make the new providers ADDITIVE and ENV-GATED —
a provider is only enabled when its keys are present, so an install without keys behaves exactly as today
and NO ONE can be locked out. Do NOT modify the existing Credentials path or the Supabase adapter/core.
If something can't be safely verified in CI, keep it behind the env gate (off by default) rather than
risking the login flow — then move on.
Branch ticket/social-login-apple-google. Gate: npm test (web vitest + mobile jest).

RESEARCH (do this first — official docs)
- Sign in with Apple: https://developer.apple.com/documentation/AuthenticationServices/implementing-user-authentication-with-sign-in-with-apple
- Sign in with Google: https://developers.google.com/identity/sign-in/web/sign-in
- Confirm the exact Auth.js (@auth/core) provider config for Apple + Google and the redirect/callback
  URLs.

CONTEXT
- apps/web/src/auth.js — the providers array (Credentials at ~line 262). Add @auth/core Apple + Google
  providers here (additive). Existing Supabase adapter + Credentials path: DO NOT change.
- Mobile auth flows through the web AuthWebView (apps/mobile/src/utils/auth/AuthWebView.jsx +
  useAuth.js) — so the web sign-in page is where the Apple/Google buttons render; the mobile webview just
  needs to handle the provider redirect/callback cleanly.
- auth_users / auth_accounts already exist (OAuth account linking lands there).

GOAL: a user can sign in / sign up with Apple or Google from the web auth page (and therefore from the
mobile auth webview). Email/password keeps working unchanged. New OAuth users get a user_profiles row
(reuse the existing ensureUserProfile lazy-create path).

SCOPE
- Add GoogleProvider + AppleProvider to auth.js, each reading client id/secret (Apple: team id, key id,
  private key / client secret JWT) from env. Gate each: if its env is unset, the provider is simply not
  added (no crash, no change to today's behavior).
- .env.example: add the Apple + Google key placeholders with comments + the callback URL pattern. NEVER
  commit real values.
- Ensure a first-time OAuth login creates the user_profiles row (reuse ensureUserProfile) so the user
  lands in onboarding/app correctly, same as a fresh credentials user.
- UI: "Continue with Apple" / "Continue with Google" buttons on the web sign-in/sign-up page; confirm the
  mobile AuthWebView completes the redirect and returns to the app authenticated.
- Tests: web vitest — providers are present ONLY when env is set (gating); the credentials path is
  unchanged; a mocked OAuth callback creates/links a user_profiles row. Keep tests hermetic (mock the
  provider network).

DO NOT: alter or weaken the existing Credentials/email-password login; rewrite the Supabase adapter; hard-
fail when keys are absent; commit secrets. No DB migration (auth_users/auth_accounts already exist).

VERIFY: npm test green. PR body: additive env-gated Apple+Google providers, credentials path untouched,
ensureUserProfile on first OAuth login, the gating tests. ⚙️ FLAG FOR TATS (test-backlog ACTION 2-style):
create the Apple Service ID + key and the Google OAuth client, set the env keys + register the callback
URLs; until then the buttons are simply absent. DEVICE/browser: with keys set, Apple + Google sign-in
complete; without keys, login is exactly as before.
```
