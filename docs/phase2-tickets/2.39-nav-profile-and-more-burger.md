# 2.39 — Instagram-style nav: bottom-right Profile + More as a burger in Profile

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile-only, frontend-only, no migration. Covers
#12 (bottom-right "my profile" button) + #13 (move "More" into the profile top-right burger). Build AFTER
2.38 (touches profile) and respect the 2.19 nav fix.

```
ORIENTATION
PawPi Expo mobile (expo-router). Phase-2 ticket 2.39: make the bottom-right tab your own Profile (like
Instagram), and move the "More" menu into a top-right burger ON the profile screen. Read
docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible Instagram-like layout and proceed. CI green → auto squash-merge;
red → fix then merge. Keep going through Wave 5. One branch off origin/main. NO migration.
Branch ticket/nav-profile-and-more-burger. Gate: npm test (mobile jest).

CONTEXT
- apps/mobile/src/app/(tabs)/_layout.jsx — bottom bar is Feed / Health / Training / Services / More.
- apps/mobile/src/app/(tabs)/more/_layout.jsx + more/index.jsx — the More landing + its stack
  (community, hub, data-access, profile, settings; service screens were moved to root by 2.19 — DO NOT
  reintroduce them into a tab stack).
- The user's own profile screen (find it — likely the dog social profile "My Dog" / a profile screen).
- 2.19 fixed More-tab corruption — keep that intact; do not push cross-tab into a stack that corrupts it.

GOAL: the bottom-right tab opens MY profile directly (one tap, like Instagram). The "More" menu
(community, hub, data-access, settings, list-your-business, etc.) becomes a top-right burger/menu button
ON the profile screen — not a bottom tab.

SCOPE
- Bottom bar becomes Feed / Health / Training / Services / Profile (Profile replaces More at bottom-right,
  with a profile/avatar icon). Tapping it opens the owner's own profile.
- The Profile screen gets a top-right burger (☰) opening the "More" menu with all the destinations
  currently under More (community, hub, data-access, settings, list-your-business, etc.). Every previous
  More destination stays reachable from here.
- Keep navigation clean: opening More destinations from the burger must not corrupt any tab (honor 2.19);
  back returns correctly. Pick the simplest structure (e.g. the burger opens a menu screen/sheet that
  routes to the existing destinations).
- Tests: the bottom-right tab routes to the profile; the burger menu lists + routes to each former-More
  destination; no nav-corruption regression (assert reset/return behavior).

DO NOT: lose any More destination; reintroduce the 2.19 corruption; add a migration; move the service
screens.

VERIFY: npm test (mobile) green. DEVICE: bottom-right opens my profile; the top-right burger reveals the
full More menu; every old More item still reachable; back behaves.
```
