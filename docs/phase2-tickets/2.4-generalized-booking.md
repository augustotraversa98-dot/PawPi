# 2.4 — Generalized booking + calendar (all capabilities)

Read `00-README.md` + `../phase2-superapp-master-plan.md` §2 first.

```
ORIENTATION
PawPi web API + mobile. Phase-2 ticket 2.4: generalize booking beyond vet so grooming/walking/daycare/
sitting/training can all be booked on one model. Today booking = vet_appointments (its booking columns
were named as a forward-compatible subset of a cross-type booking). Read docs/phase2-tickets/00-README.md
+ master plan §2 + §5, 0016 (vet_appointments booking columns), 0017 (booking_status CHECK), the
booking routes (providers/[id]/book, /bookings, /bookings/[appointmentId]), 2.1 capabilities, and 2.3
payments (a booking can require a deposit).
Branch ticket/generalized-booking off origin/main; squash-merge; migration hand-applied after merge.
Gates: npm test + npm run test:integration. ⚠️ Do NOT break the existing vet booking flow or its
reminder integration.

GOAL: one booking model for all capabilities — pick a provider + capability + service + slot (+ staff,
location, recurrence) → request → confirm/decline → optional deposit (2.3). Vet keeps working.

SCOPE — DECISION FIRST: either (a) extend vet_appointments into a general "provider_bookings" (rename/
view + a capability/service_kind column) keeping vet rows working, or (b) add a provider_bookings table
and make vet_appointments a special case. Choose the lower-risk path that does NOT disturb the vet
reminder engine; DOCUMENT the choice in the PR.
- Booking carries: provider_id, capability/service_kind, service_id (from provider_services),
  location_id, staff_user_id, pet_id, owner_user_id, start/end or slot, status (requested/confirmed/
  declined/cancelled/completed), source, recurrence (rule for recurring walks/daycare), deposit/payment
  link (order_id from 2.3, nullable).
- Availability/calendar: per-provider/staff availability + slot generation; prevent double-book;
  expose a 2-way calendar sync hook (stub external sync behind an interface — full Google/Apple sync
  can be a follow-up).
- Routes: owner book (any published provider+capability), provider inbox + confirm/decline/cancel/
  assign/complete (reuse the 6b pattern), availability read. "completed" status powers reviews (2.2).
- RLS: bookings owner-scoped + provider-staff-of-provider scoped (reuse app_is_active_staff_of); harness
  proofs. Keep vet medical access via assertCareAccess unchanged.
- mobile: the existing book flow generalizes to any capability's services (reuse BookingFormModal).
- Vitest + integration: vet path still green; a non-vet capability can be booked; double-book
  prevented; recurrence; deposit link; RLS.

DO NOT: break vet_appointments/reminders; merge medical access into booking access; build full external
calendar sync (stub it). 

VERIFY: npm test + integration green incl. UNCHANGED vet booking + reminder tests. PR body: the model
decision, recurrence, deposit hook, RLS. Migration hand-applied after merge. Confirm origin/main.
```
