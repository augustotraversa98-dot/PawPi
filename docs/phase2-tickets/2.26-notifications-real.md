# 2.26 — Notifications on real data

Read `00-README.md` first. Depends on: the social actions (paw/bark/follow) + the reminder engine.
**No hard blocker**, but build alongside/after 2.25 (same feed area). Adds migration **0044**
(`notifications` table). The mock screen shows the SHAPE we want (Walks/Feeding/Paws/Barks/Training) —
back it with real events.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.26: replace the MOCK notifications with real ones. Read
docs/phase2-tickets/00-README.md.
Branch ticket/notifications-real off origin/main; squash-merge.
⚙️ NEW MIGRATION 0044 (notifications table). Gates: npm test (web vitest + mobile jest) + npm run
test:integration (notifications RLS proofs + guard). Confirm baselines first.

CONTEXT (read before editing)
- apps/mobile/src/app/notifications.jsx — TODAY loads mockNotifications from @/data/mockNotificationsData
  into useSocialPetStore; the bell already ALSO shows REAL reminder notifications (reminder engine). Filter
  chips: All / Walks / Feeding / Paws / Barks / Training. Keep the UI; make the social ones real.
- apps/web/src/app/api/posts/[id]/paw + posts/[id]/barks + pets/[id]/follow — the social ACTIONS that
  should generate a notification to the content owner.
- There is NO notifications backend today — this ticket adds one. Reminder notifications stay as-is (the
  bell merges DB social notifications + the local reminder notifications, as it already merges sources).

GOAL: a user sees real notifications about activity on THEIR content — someone pawed their post, barked
(commented), or followed their pet — plus the existing reminder notifications, filterable. Tapping a
notification opens the relevant post/profile. No mock, empty → "No notifications yet".

SCOPE
1. Migration 0044 — notifications (id, recipient_user_id → user_profiles, actor_user_id, type
   [paw|bark|follow|…], subject_ref [post id / pet id as text], body text, read_at, created_at). ENABLE+
   FORCE RLS, OWNER-SCOPED: recipient_user_id = current_app_user_id() for SELECT/UPDATE(read stamp);
   INSERT is done server-side by the action routes under the actor's identity but must target the
   recipient — use a SECURITY DEFINER insert helper (app_notify(recipient, actor, type, subject_ref,
   body)) so the actor (not the recipient) can create the row without a cross-user write policy (mirror
   the consent/booking helper pattern; pinned search_path; GRANT EXECUTE). No DELETE (or owner-only).
   Harness proofs: recipient reads own; another user reads ZERO; the DEFINER helper inserts for a
   recipient the actor is not. Completeness guard classifies it.
2. Wire app_notify() into the paw / bark / follow routes (fire-and-don't-block: a failed notify never
   fails the action; never notify yourself for your own action). 
3. Read route GET /api/notifications (recipient-scoped, newest first, paginated) + a mark-read route.
4. Mobile: notifications.jsx pulls real social notifications from the API + merges the existing reminder
   notifications; the filter chips work over the merged set; tap-through opens the post/profile; unread
   styling + mark-read. Remove the mockNotificationsData seeding.
5. Tests: web vitest (a paw/bark/follow inserts a notification for the content owner, not the actor; GET
   is recipient-scoped; mark-read); integration (notifications RLS proofs + the DEFINER insert helper +
   guard); mobile jest (renders merged real + reminder notifications, filters, empty state, tap-through).

DO NOT: keep mock notifications as a fallback; let a notify failure break the underlying social action;
notify a user about their own action; allow cross-user reads; touch the reminder-notification path beyond
merging. No fake data.

VERIFY: npm test (mobile+web) + integration green (baseline + net-new). PR body: notifications table +
owner RLS + the app_notify DEFINER insert, wiring into paw/bark/follow, the merged bell, mock removed.
⚙️ FLAG FOR TATS: apply 0044. DEVICE PASS: have someone paw/bark/follow your content → a real notification
appears and taps through; empty account shows the empty state.
```
