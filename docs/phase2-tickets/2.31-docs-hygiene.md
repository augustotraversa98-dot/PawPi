# 2.31 — Docs hygiene: ARCHITECTURE.md + SCHEMA_NOTES.md refresh

Read `00-README.md` first. **Docs-only, NO code, NO migration, NO tests changed.** Independent — no
blocker, slot in anytime. Keeps the two files Code reads for orientation accurate. Closes the roadmap
follow-up *"Docs hygiene: ARCHITECTURE.md says a standalone useCurrentPet.js exists … ; SCHEMA_NOTES.md
migration-order line stops at 0011. Refresh both."*

```
ORIENTATION
PawPi repo docs. Phase-2 ticket 2.31: fix two stale orientation docs. Read docs/phase2-tickets/00-README.md.
Branch ticket/docs-hygiene off origin/main; squash-merge. NO migration, NO code, NO test changes (CI
should be a no-op beyond the unchanged baselines).

THE TWO STALE SPOTS (verify against the repo first, then correct)
1. ARCHITECTURE.md — describes TWO `useCurrentPet` hooks (a standalone `src/hooks/useCurrentPet.js` AND
   one in `usePetProfile.js`) with "slightly different return shapes" (around lines 176–178, 185–186, and
   the open-question at ~244). This was UNIFIED long ago (PR #12 — single `useCurrentPet`; the standalone
   `useCurrentPet.js` no longer exists; it lives in `usePetProfile.js`). Confirm by checking the repo
   (there is no `src/hooks/useCurrentPet.js`), then rewrite those passages to describe the SINGLE
   `useCurrentPet` in `usePetProfile.js` and remove the "two hooks / which is authoritative" open question
   + the duplication caveat. Also fix any line/path references that no longer match.
2. supabase/SCHEMA_NOTES.md — the "Migration order" line (~line 7) stops at `0011_reminder_dismissals`.
   The repo now has migrations through the RLS arc (0012–0026), Phase 2 (0027–0038), and the Wave 3/4
   tickets (0039–0045, applied as those land). Update the migration-order summary to reflect the current
   range — list them or, better, replace the hand-maintained enumeration with a short note + a pointer to
   `supabase/migrations/` as the source of truth (so it doesn't go stale again), and keep the per-migration
   call-outs that matter. Make sure the identity/jsonb/RLS notes still read correctly.

SCOPE
- Edit ONLY ARCHITECTURE.md + supabase/SCHEMA_NOTES.md. Verify each claim against the actual repo before
  changing it (grep for `useCurrentPet`, `ls supabase/migrations`). Don't invent history — describe what's
  there now.
- While in SCHEMA_NOTES.md, sanity-check that the other migration-order / re-runnable notes are still
  accurate; fix anything obviously stale you touch, but keep scope to these two files.
- No code, no migration, no test edits.

DO NOT: change any code or schema; rewrite the whole docs (surgical corrections only); remove still-valid
content; introduce claims you haven't verified against the repo.

VERIFY: npm test + npm run test:integration unchanged/green (this PR should not affect them). PR body: the
two corrected passages (single useCurrentPet; migration-order through current + a pointer to the migrations
dir as source of truth), confirmed against the repo. No device check.
```
