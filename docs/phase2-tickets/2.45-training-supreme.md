# 2.45 — Training section, supreme (a real multi-session curriculum)

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + web API. Covers #3. New content + progress
tracking → migration. Research-backed.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.45: make the self-Training tab a SUPREME, structured
curriculum owners progress through — many sessions to complete. Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; RESEARCH a credible curriculum online and pick a strong, comprehensive structure,
then proceed. CI green → auto squash-merge; red → fix then merge. Keep going through Wave 5. One branch
off origin/main.
Branch ticket/training-supreme. Gates: npm test (web vitest + mobile jest); npm run test:integration
(RLS).

RESEARCH (do this first)
- Deep-dive reputable dog-training sources (AKC, professional trainers, puppy/obedience programs) and
  design a real curriculum: multiple PROGRAMS (e.g. Puppy Essentials, Basic Obedience, Leash Manners,
  Recall, Impulse Control, Tricks, Socialization, Senior-dog enrichment), each with an ordered list of
  SESSIONS/lessons (objective, steps, tips, duration, difficulty). Make the content genuinely useful and
  substantial — "many sessions to complete." This is the DIY self-training tab, DISTINCT from the
  provider "hire a trainer" service (2.10), which must stay separate (keep the existing "Want a pro?"
  banner linking to it).

CONTEXT
- apps/mobile/src/app/(tabs)/training.jsx — currently renders TRAINING_LESSONS from mockData (replace).
- 2.10 provider training is separate (training_programs/sessions/progress are PROVIDER-side enrollments) —
  do NOT reuse those tables for the self-curriculum; this is owner self-paced content.

GOAL: an owner browses programs, works through their ordered sessions, marks sessions complete, and sees
progress (e.g. "4 of 12 done", streaks/badges optional), per the active pet. Content is real and curated;
progress persists per owner+pet.

SCOPE
- Curriculum content: ship the programs + sessions as structured app content (a static, versioned
  content module is fine — it's curated material, not user data, so no "fake data" concern). Make it rich.
- Migration (next free number ≥0046): self-training PROGRESS only — e.g. training_progress_self
  (owner_user_id, pet_id, program_key, session_key, completed_at, unique per owner+pet+session) so
  completion persists per pet. ENABLE+FORCE RLS owner-scoped (owner reads/writes their own; others zero).
  Harness proofs + completeness guard. Flag migration in test-backlog ACTION 1. (Name it distinctly so it
  never collides with 2.10's provider training_progress.)
- Mobile: program list → session list (locked/▸ in order or freely browsable — pick sensible) → session
  detail with the content + "Mark complete" → progress reflected (per active pet, switches with the pet).
  Remove the TRAINING_LESSONS mock. Keep the "Want a pro? Hire a trainer" banner → provider training.
- Routes (web): read/write self-training progress (owner-scoped).
- Tests: web vitest (progress upsert owner-scoped, completion idempotent); integration (owner-only RLS
  proofs); mobile jest (browse curriculum, mark complete, progress count, per-pet scoping, mock removed,
  the pro banner still links to the service).

DO NOT: merge with the provider training tables; treat curated lesson CONTENT as "fake data" (it's curated
material — fine); leave the mock; let progress leak across users/pets. Migration hand-applied by Tats.

VERIFY: npm test + integration green. PR body: the researched curriculum (programs+sessions), the
owner+pet progress model + RLS, self-vs-provider separation. DEVICE: complete sessions → progress sticks
per pet; switching pets switches progress; the pro banner still works.
```
