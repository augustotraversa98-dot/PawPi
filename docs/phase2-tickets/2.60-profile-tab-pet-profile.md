# 2.60 — Bottom "Profile" tab → my pet's social profile + pet-photo avatar icon

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Mobile only. NO migration. Refines
2.39. ⛔ Build AFTER 2.59 (shared `app/(tabs)/_layout.jsx`) and AFTER 2.55 (reuses its avatar fallback).

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile app.
Phase-2 ticket 2.60: make the bottom-right tab open the active pet's SOCIAL profile (the "Pet profile"
screen) and show the pet's PHOTO as the tab icon (paws glyph when there's no photo). Read
docs/phase2-tickets/00-README.md (autonomy preamble; no-fake-data rule). Sequence after 2.59 + 2.55.
AUTONOMY: don't ask; pick the sensible, on-brand option and proceed. CI green → auto squash-merge; red →
fix then merge. One fresh branch off origin/main: ticket/profile-tab-pet-profile.
Gates: npm test (mobile jest).

CONTEXT
- Today the bottom-right tab (2.39) opens the OWNER landing (app/(tabs)/more/index.jsx — the orange owner
  card + My Dogs switcher), and the former "More" menu is behind the top-right ☰ burger. The actual pet
  SOCIAL profile (app/pet-profile.jsx — the screen in Tats's first screenshot: avatar, @handle, "with
  {owner}", the Daily posts/Paws/Barks/Followers/Following stat row, Daily moments grid, About) is
  currently only reachable by posting and tapping the pet name. Tats wants the bottom tab to land directly
  on THAT pet social profile for the active pet.
- The active pet comes from the reactive useCurrentPet (single source of truth; 2.34). pet-profile.jsx
  already loads a pet's social profile by petId.
- 2.55 added a shared avatar fallback (photo → initials/paws). REUSE it for the tab icon.
- The 2.19 nav fix + 2.39 burger must stay intact: the former More-menu destinations (Community, My Hub,
  Dog Profile, Reminders & Routines, Settings, Reset) must remain reachable from the ☰ burger.

GOAL
The bottom-right tab is the active pet's social Profile: its icon is the pet's profile PHOTO (a small
circular avatar; paws glyph when no photo, via the 2.55 fallback), and tapping it opens the pet social
profile (pet-profile.jsx) for the current pet. The owner-level stuff (My Dogs switcher, the former More
menu) stays reachable (burger + within the profile as today) — nothing is lost. Switching the active pet
updates both the tab icon and the profile shown.

SCOPE
- app/(tabs)/_layout.jsx: the 5th tab renders a circular pet-photo avatar as its icon (active pet's photo
  from useCurrentPet; 2.55 fallback when none), active-state ring/emphasis on-brand. Keep it the same tab
  slot so the 2.19 nav fix + popToTopOnBlur stay intact.
- Make the 5th tab show the active pet's SOCIAL profile. Cleanest: have the tab's screen render the pet
  social profile for the current pet (reuse the pet-profile view/component for petId = current pet) rather
  than the owner card; OR have it route to pet-profile for the current pet. Pick the approach that keeps
  the tab root stable (no nav corruption) and keeps the ☰ burger + My Dogs switcher reachable. The former
  More-menu destinations MUST remain reachable (keep the burger).
- Keep the top-right ☰ burger (2.39) with all former More destinations; keep the My Dogs switcher reachable
  (within the profile or the burger). Nothing from the old More section is orphaned.
- Empty/no-pet state: if there's no active pet, the tab shows the paws fallback and the screen shows the
  existing no-pet state (create/add a pet), not a crash.
- Tests: mobile jest (the 5th tab renders the pet-photo avatar when a photo exists, paws fallback when not;
  tapping opens the pet social profile for the active pet; switching the active pet updates icon + screen;
  the burger still exposes every former More destination; no-pet fallback).

DO NOT: rebuild the pet-profile screen (reuse it); orphan any former More-menu destination (keep the
burger); break the 2.19 nav fix / 2.39 structure / popToTopOnBlur; introduce fake pet data; hardcode the
avatar (use useCurrentPet + the 2.55 fallback).

VERIFY: npm test green. PR body: the pet-photo avatar tab icon (with 2.55 fallback), the tab opening the
active pet's social profile, the burger/My-Dogs reachability preserved, the no-pet state. DEVICE: the
bottom-right tab shows your pet's photo and opens your pet's social profile (the screenshot-1 screen) in
one tap; switch the active pet → the icon + profile update; the ☰ still reveals Community/Hub/Dog Profile/
Reminders/Settings; a fresh account with no pet shows the paws fallback + add-a-pet state.
```
