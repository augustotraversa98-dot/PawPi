# 2.25 — Search & Discover on real data

Read `00-README.md` first. Depends on: the existing posts/pets/follow + provider discovery routes.
**Mobile-led, no migration** (reads existing tables). Independent — no blocker. Part 1 of the "feed real
data" item; the mock screens already show the SHAPE we want — back them with real data.

```
ORIENTATION
PawPi Expo mobile + web API. Phase-2 ticket 2.25: replace the MOCK Search/Discover with real data. Read
docs/phase2-tickets/00-README.md.
Branch ticket/feed-search-discover-real off origin/main; squash-merge. NO migration.
Gates: npm test (mobile jest) + npm test (web vitest if you add/extend a route). Confirm baselines first.

CONTEXT (read before editing)
- apps/mobile/src/app/search.jsx — TODAY loads mockPopularProfiles + mockPopularPetMoments from
  @/data/mockDiscoveryData. The layout (popular profiles, popular pet moments, a search box for "dogs,
  breeds, pet parents") is the intended shape — keep the UI, swap the data source.
- apps/web/src/app/api/posts/route.js (feed posts), pets/[id]/follow, providers/discover, and the
  pet-profile endpoint (/api/pets/[id]/profile, stats + moments) — the real sources.
- Pets/posts are real + RLS'd (pets any-authed read; posts public-read window). Provider discovery exists.

GOAL: Search returns real pets/owners/breeds and real provider businesses; Discover shows real popular
pet profiles + real pet moments (posts) — no mock. Tapping a result opens the real pet profile / provider
storefront / post. Empty/no-results → clean empty states.

SCOPE
1. Search backend: add/extend a search route (web) that, given a query, returns matching pets (name/
   breed/handle), owners (username/name), and providers (name/capability) — published/public fields only,
   RLS-scoped. Keep it simple (ILIKE on the relevant columns, sane limits). Providers reuse the discovery
   pattern.
2. Discover backend: "popular profiles" = pets ranked by a real signal (followers and/or paws received —
   reuse the pet-profile stats sources); "popular pet moments" = recent/real posts (reuse /api/posts).
   Public fields only. Pagination + limits.
3. Mobile: delete the mockDiscoveryData usage in search.jsx; wire React Query to the real endpoints
   (debounced search; discover sections on first render). Tap-through: pet → pet profile; provider →
   storefront (2.22); moment → the post. Empty states ("No results", "Nothing here yet").
4. Remove (or stop importing) @/data/mockDiscoveryData once nothing references it.
5. Tests: web vitest (search returns pets/owners/providers, public fields only, RLS-scoped, isolation);
   mobile jest (search.jsx renders real results from a mocked endpoint, debounce, empty states, tap-through
   routes).

DO NOT: keep any mock fallback "so it's not empty" (empty → empty state, project rule); leak private
fields in search; add a migration; build a heavyweight search engine (DB ILIKE is enough for now).

VERIFY: npm test (mobile + web) green (baseline + net-new). PR body: real search + discover sources, mock
data removed, tap-through to profile/storefront/post, empty states, RLS-scoped public-only. DEVICE PASS
(docs/test-backlog.md): search a real dog/owner/business → real results; Discover shows real popular
profiles + moments; no-results shows an empty state.
```
