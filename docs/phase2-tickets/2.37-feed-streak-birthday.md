# 2.37 — Feed delight: posting streak + birthday frame

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile + a small web read. Covers #11.7 (streak
fire emoji) + #11.8 (birthday emoji + highlighted frame). Build AFTER 2.36 (same feed/PostCard files).

```
ORIENTATION
PawPi Expo mobile + web API (read). Phase-2 ticket 2.37: posting streak + birthday highlight on the feed.
Read docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5. One branch off origin/main. Build after 2.36 is merged (shared feed
files).
Branch ticket/feed-streak-birthday. Gates: npm test (web vitest if you add a read; mobile jest);
integration only if a DB/RLS path changes.

GOAL
1. (#11.7) Show a 🔥 fire emoji + streak count next to the pet/owner name = consecutive days with a daily
   post. If the user has NOT posted on the CURRENT day, the streak resets to 1 (i.e. a missed day breaks
   the chain; the count reflects the current consecutive run including today once they've posted).
2. (#11.8) If the pet has a birthday OR adoption date that is TODAY, show a 🎂 birthday emoji by the name
   AND render that day's daily-photo frame in the app's signature ORANGE (instead of the default) to
   highlight it.

CONTEXT
- Streak source = the posts table (per owner/pet, by created day, viewer's local day boundary). Compute
  it cheaply: a small read/aggregate (web) or derive client-side from the user's recent posts. Pick the
  simplest correct approach; keep the day-boundary logic pure + tested (timezone-aware, local day).
- Birthday source = the pet's birthday/adoption date (Dog Profile fields). CONFIRM the columns exist on
  pets; if a date field is genuinely missing, add a nullable column (migration — flag it) — but check
  first, they likely exist.
- apps/mobile/src/components/Feed/PostCard.jsx (+ the share frame / DailyPromptCard) — where the name +
  frame render. The share frame from 2.28 (ShareableDailyCard) may also want the birthday color.

SCOPE
- Pure streak helper (consecutive-day run incl. today; resets to 1 when today breaks the chain) + wire the
  🔥+count next to the name. Pure birthday helper (today == birthday or adoption date, ignoring year) +
  wire the 🎂 + orange frame.
- Tests: streak helper (continuous run, missed-day reset, no-post-today behavior, TZ boundary); birthday
  helper (matches today regardless of year; no false positive); render tests for the emoji + orange frame.

DO NOT: fabricate a streak when there are no posts (0/none → no fire badge); show a birthday when there's
no date; add a migration unless a pet date column is truly missing (check first). No fake data.

VERIFY: npm test (+ integration if DB touched) green. DEVICE: streak shows + resets correctly; on a pet's
birthday/adoption day the 🎂 + orange frame appear.
```
