# 2.59 — Floating Instagram-style tab bar (not static at the bottom)

Read `00-README.md` (Wave 6 order + the ⚡ autonomy preamble) first. Mobile only. NO migration. Pure
restyle of the bottom tab bar into a floating rounded pill (like Instagram Reels' nav). ⛔ Touches
`app/(tabs)/_layout.jsx` — sequence with 2.60 (which also edits it); build 2.59 first.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile app.
Phase-2 ticket 2.59: restyle the bottom tab bar as a FLOATING pill (rounded, detached from the bottom
edge, subtle shadow) instead of a static full-width bar. Read docs/phase2-tickets/00-README.md (autonomy
preamble; warm/modern design language). Reference: Instagram's floating nav (the second screenshot Tats
shared).
AUTONOMY: don't ask; pick the sensible, on-brand option and proceed. CI green → auto squash-merge; red →
fix then merge. One fresh branch off origin/main: ticket/floating-tab-bar.
Gates: npm test (mobile jest).

CONTEXT
- The tab bar lives in app/(tabs)/_layout.jsx. The current structure (5 tabs) and the navigation behavior
  must NOT change — this is purely visual. The 2.19 nav fix (service screens in a root-level stack;
  popToTopOnBlur per tab) and the 2.39 structure (bottom-right = Profile, top-right ☰ burger) MUST stay
  intact. (2.60 will adjust what the Profile tab opens + its icon — keep this PR visual-only so they
  compose cleanly.)
- The app is warm/cream/coral. The floating bar should feel native and on-brand (not a literal IG clone) —
  a rounded container with horizontal margin + a bottom inset above the home indicator, a soft shadow, and
  the active tab highlighted.

GOAL
The bottom tab bar floats: a rounded pill centered with side margins, lifted off the bottom safe-area edge
(sits above the home indicator), with a subtle shadow/elevation and the active tab clearly indicated.
Tapping behavior, routes, and the number/order of tabs are unchanged. It reads cleanly over scrolling
content and respects the safe area on notch + non-notch devices.

SCOPE
- In app/(tabs)/_layout.jsx, customize the Tabs bar styling (tabBarStyle: position 'absolute', rounded
  borderRadius, horizontal margins, bottom offset using safe-area insets, background + shadow/elevation,
  no top hairline border) — or a lightweight custom tabBar renderer if cleaner. Keep all five tabs and
  their existing icons/labels/routes.
- Ensure scrollable screens get bottom padding so the floating bar never covers the last row of content
  (add the needed contentInset/padding where lists currently assume a static bar height).
- Respect safe-area insets (useSafeAreaInsets) so it floats correctly on devices with/without a home
  indicator. Keep it accessible (hit targets ≥44pt).
- Tests: mobile jest (the five tabs still render in order; the active tab is indicated; the existing
  _layout.test.jsx assertions still pass — update only for the styling/render shape, not behavior).

DO NOT: change routes, tab count/order, or any navigation behavior; break the 2.19 nav fix or the 2.39
Profile/burger structure; let the floating bar cover list content (add the padding); hardcode device
sizes (use safe-area insets).

VERIFY: npm test green. PR body: the floating tab-bar styling + the safe-area handling + the list-padding
adjustment; confirm 2.19/2.39 behavior preserved. DEVICE: the nav floats as a rounded pill above the home
indicator with a shadow; the active tab is clear; scrolling content isn't hidden behind it; all five tabs
navigate exactly as before on notch + non-notch devices.
```
