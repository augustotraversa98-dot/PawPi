# 2.38 — Profile fixes: share button + real timestamps

Read `00-README.md` (Wave 5 autonomy preamble) first. Mobile-only. Covers #11.9 (share button broken on
own profile) + #11.10 ("just now" fake timestamp). Build AFTER 2.37 (shared PostCard / share-frame).

```
ORIENTATION
PawPi Expo mobile. Phase-2 ticket 2.38: fix the profile share button + post timestamps. Read
docs/phase2-tickets/00-README.md.
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. Keep going through Wave 5. One branch off origin/main. NO migration.
Branch ticket/profile-fixes. Gate: npm test (mobile jest).

TWO BUGS
1. (#11.9) On your own profile, viewing your posts, the SHARE button does nothing. It should share (reuse
   the 2.28 ShareableDailyCard / share flow — capture + system share sheet).
2. (#11.10) A post shows "just now" when it was actually posted an hour ago — a stale/mock relative-time.
   Show the real relative time from the post's created_at.

CONTEXT
- The profile/own-posts screen (find it — pet-profile.jsx and/or the More→profile screen) renders the
  user's posts with a share button + a timestamp.
- 2.28 added ShareableDailyCard + the share handler (react-native-view-shot + expo-sharing) on the feed
  PostCard — reuse that exact flow for the profile share button.
- The "just now" likely comes from hardcoded/mock text or a relative-time util fed the wrong value.

GOAL: the profile share button shares the post (same branded flow as the feed), and every post shows its
true relative time (e.g. "1h", "yesterday") computed from created_at.

SCOPE
- Wire the profile share button to the existing share flow (reuse ShareableDailyCard / the 2.28 handler;
  don't build a second share path).
- Replace the hardcoded/wrong timestamp with a real relative-time formatter fed the post's created_at
  (reuse any existing time util; if none, a small pure helper + test).
- Tests: share button invokes the share flow (mock capture/sharing); relative-time helper returns correct
  buckets ("just now" only when truly recent, "1h", "yesterday", dates).

DO NOT: build a separate share implementation; show fake timestamps; add a migration.

VERIFY: npm test (mobile) green. DEVICE: profile share opens the share sheet; timestamps are real.
```
