# 2.67 — Fix: Followers/Following opens "+not-found" (route not resolving on device)

Read `00-README.md` first. Mobile only. NO migration. Device-found follow-up to 2.61 (Part A). Small,
surgical.

```
ORIENTATION
Read ARCHITECTURE.md and supabase/SCHEMA_NOTES.md to get oriented, then this task. PawPi Expo mobile app.
Phase-2 ticket 2.67: tapping Followers / Following on the pet social profile lands on the expo-router
"+not-found" screen ("Uh-oh! This screen doesn't exist (yet)") on device, instead of the follows list
shipped in 2.61. Read docs/phase2-tickets/00-README.md (autonomy preamble).
AUTONOMY: don't ask; pick the sensible option and proceed. CI green → auto squash-merge; red → fix then
merge. One fresh branch off origin/main: ticket/followers-following-route-fix.
Gates: npm test (mobile jest).

CONTEXT / WHAT'S ALREADY THERE (verify, don't assume)
- The route file app/follows.jsx exists, is committed, and is registered in app/_layout.jsx
  (<Stack.Screen name="follows" />). app/pet-profile.jsx → openFollows() does
  router.push({ pathname: "/follows", params: { petId, rel, petName } }) from the Followers/Following
  StatPills. So structurally the route is present yet device hits +not-found.
- The Profile TAB is pet-profile in EMBEDDED mode ((tabs)/more/index.jsx renders <PetProfileScreen
  embedded />). In embedded mode petId = params.petId || String(currentPet?.id ?? ""). If currentPet
  hasn't loaded, petId is "" and openFollows early-returns (no nav) — a separate "nothing happens" bug.
- Likely root cause #1 (most probable): adding a NEW route file requires a Metro restart with --clear;
  a stale dev bundle won't have /follows in its route tree → +not-found. Confirm by reproducing on a
  CLEAN start (npx expo start --clear) BEFORE changing code. If a clean start fixes it, the code is fine
  — then ONLY do the robustness items below + document it.
- Likely root cause #2 (if it persists after --clear): the navigation target/params or route registration
  doesn't resolve from inside the (tabs) group (embedded), or the push fires with an empty petId.

GOAL
Tapping Followers or Following reliably opens the 2.61 follows list (for the right pet + rel) from BOTH
entry points — the standalone pet-profile route AND the embedded Profile tab — with no +not-found and no
dead taps. The list loads for the active pet.

SCOPE (surgical — keep it minimal)
- Reproduce first on a clean `expo start --clear`. Document in the PR whether the bug was cache-only or a
  real routing bug.
- Make navigation robust regardless:
  • In pet-profile openFollows, resolve the pet id from petId OR the embedded fallback (viewerPetId /
    currentPet?.id) so the embedded tab never pushes with an empty petId; if it's still unresolved
    (currentPet loading), disable the pills / no-op gracefully rather than a dead tap.
  • Confirm /follows resolves from within the (tabs) group. If a router.push({pathname}) from the tab is
    the issue, use the form that reliably targets the root route (e.g. an absolute href string
    `/follows?petId=...&rel=...` or router.navigate) — pick what works on device.
  • Verify follows.jsx reads petId/rel from useLocalSearchParams and falls back to the current pet when
    petId is absent, so a paramless open still shows the active pet's lists.
- Confirm the route is genuinely registered (file present + _layout entry) and that nothing else (a typed-
  routes manifest, a duplicate name, header options) shadows it.
- Tests: mobile jest (tapping Followers/Following calls navigation with a NON-empty petId + correct rel
  from both standalone and embedded mounts; follows screen renders for a param petId AND falls back to the
  current pet when no petId; no dead tap while currentPet is loading).

DO NOT: introduce a new route group/restructure the nav (keep the 2.19 fix + 2.59/2.60 intact); add a
migration; push with an empty petId; change the follows list UI/behavior (this is a routing fix only).

VERIFY: npm test green. PR body: the reproduction finding (cache vs code), the robust navigation +
embedded-petId fallback, and confirmation /follows resolves from both entry points. DEVICE (clean
--clear start): from your pet's profile (via the Profile tab AND via tapping a pet name from a post),
tap Followers and Following → the searchable list opens for the right pet; no +not-found; no dead taps;
the follow/unfollow toggle + row→profile still work.
```
