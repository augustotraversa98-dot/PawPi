// TEMP diagnostic (2.77 tab fix). A unique build token so we can confirm the
// device is running THIS JS bundle (not a stale/cached one) — logged at boot and
// shown as a tiny dev-only tag on the Feed header. Bump the token when you want a
// fresh, unmistakable marker. Safe to remove once the device is verified.
export const BUILD_MARKER = "2.77-tabfix-Δ7";
